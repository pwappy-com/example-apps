function initSoundEngine(){let audioCtx=null;let soundEnabled=!0;try{const saved=localStorage.getItem('floppy_sound_enabled');if(saved!==null){soundEnabled=(saved==='true')}}catch(e){}
function getCtx(){if(!audioCtx){const AudioContext=window.AudioContext||window.webkitAudioContext;if(AudioContext){audioCtx=new AudioContext()}}
if(audioCtx&&audioCtx.state==='suspended'){audioCtx.resume()}
return audioCtx}
window.FloppySound={isEnabled:function(){return soundEnabled},toggle:function(){soundEnabled=!soundEnabled;try{localStorage.setItem('floppy_sound_enabled',soundEnabled?'true':'false')}catch(e){}
return soundEnabled},set:function(val){soundEnabled=!!val;try{localStorage.setItem('floppy_sound_enabled',soundEnabled?'true':'false')}catch(e){}
return soundEnabled},playThrow:function(chargeRate){if(!soundEnabled)return;const ctx=getCtx();if(!ctx)return;try{const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='sine';const baseFreq=200+(chargeRate||0)*150;osc.frequency.setValueAtTime(baseFreq,ctx.currentTime);osc.frequency.exponentialRampToValueAtTime(baseFreq*2.5,ctx.currentTime+0.15);gain.gain.setValueAtTime(0.3,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.01,ctx.currentTime+0.15);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.16)}catch(e){}},playHit:function(){if(!soundEnabled)return;const ctx=getCtx();if(!ctx)return;try{const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='triangle';osc.frequency.setValueAtTime(140,ctx.currentTime);osc.frequency.exponentialRampToValueAtTime(40,ctx.currentTime+0.2);gain.gain.setValueAtTime(0.4,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.01,ctx.currentTime+0.2);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.21)}catch(e){}},playDive:function(){if(!soundEnabled)return;const ctx=getCtx();if(!ctx)return;try{const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='sine';osc.frequency.setValueAtTime(550,ctx.currentTime);osc.frequency.exponentialRampToValueAtTime(180,ctx.currentTime+0.3);gain.gain.setValueAtTime(0.35,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.01,ctx.currentTime+0.3);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.31)}catch(e){}},playTackleHit:function(){if(!soundEnabled)return;const ctx=getCtx();if(!ctx)return;try{const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='triangle';osc.frequency.setValueAtTime(220,ctx.currentTime);osc.frequency.exponentialRampToValueAtTime(30,ctx.currentTime+0.28);gain.gain.setValueAtTime(0.55,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.01,ctx.currentTime+0.28);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.29)}catch(e){}},playGetUp:function(){if(!soundEnabled)return;const ctx=getCtx();if(!ctx)return;try{const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='sine';osc.frequency.setValueAtTime(280,ctx.currentTime);osc.frequency.exponentialRampToValueAtTime(560,ctx.currentTime+0.18);gain.gain.setValueAtTime(0.3,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.01,ctx.currentTime+0.18);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.19)}catch(e){}},playWhistle:function(){if(!soundEnabled)return;const ctx=getCtx();if(!ctx)return;try{const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='sawtooth';osc.frequency.setValueAtTime(800,ctx.currentTime);osc.frequency.setValueAtTime(1200,ctx.currentTime+0.1);gain.gain.setValueAtTime(0.2,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.01,ctx.currentTime+0.4);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.41)}catch(e){}},playFinishFanfare:function(){if(!soundEnabled)return;const ctx=getCtx();if(!ctx)return;try{const notes=[523.25,659.25,783.99,1046.50];notes.forEach(function(freq,i){const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='triangle';osc.frequency.setValueAtTime(freq,ctx.currentTime+i*0.12);gain.gain.setValueAtTime(0,ctx.currentTime+i*0.12);gain.gain.linearRampToValueAtTime(0.35,ctx.currentTime+i*0.12+0.02);gain.gain.exponentialRampToValueAtTime(0.001,ctx.currentTime+i*0.12+(i===3?1.2:0.4));osc.connect(gain);gain.connect(ctx.destination);osc.start(ctx.currentTime+i*0.12);osc.stop(ctx.currentTime+i*0.12+1.3)})}catch(e){}},playCountdown:function(isGo){if(!soundEnabled)return;const ctx=getCtx();if(!ctx)return;try{const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='sine';const freq=isGo?880:440;osc.frequency.setValueAtTime(freq,ctx.currentTime);gain.gain.setValueAtTime(0.3,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.01,ctx.currentTime+0.3);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+0.31)}catch(e){}}}}
window.initSoundEngine=initSoundEngine;function initFloppyState(){let savedName='ゆきんこ';let savedColor='#38bdf8';try{savedName=localStorage.getItem('floppy_player_name')||'ゆきんこ';savedColor=localStorage.getItem('floppy_player_color')||'#38bdf8'}catch(e){}
if(!window.FloppyState){window.FloppyState={playerName:savedName,playerColor:savedColor,peer:null,myPeerId:'',isHost:!1,activeRoom:null,connections:{},discoveredRooms:{},isBotMatch:!1,currentGameSettings:null,hostConn:null}}else{window.FloppyState.playerName=savedName;window.FloppyState.playerColor=savedColor}
if(window.initSoundEngine){window.initSoundEngine()}}
window.initFloppyState=initFloppyState;function initHomePage(event){if(window.initFloppyState){window.initFloppyState()}
if(window.cleanupHostRoom&&(!window.FloppyState||!window.FloppyState.activeRoom)){window.cleanupHostRoom()}
window._cachedDiscoveredRooms={};const page=event.target||document.querySelector('#home-page');if(!page)return;const content=page.querySelector('.page__content');if(content){content.style.touchAction='pan-y';content.style.overflowY='auto'}
if(window.setupHomeCharacterPreview){window.setupHomeCharacterPreview(page)}
if(window.FloppySound){const isSoundOn=window.FloppySound.isEnabled();const soundIcon=page.querySelector('#sound-icon');const soundText=page.querySelector('#sound-toggle-text');if(soundIcon){soundIcon.className=isSoundOn?'fa-solid fa-volume-high':'fa-solid fa-volume-xmark'}
if(soundText){soundText.textContent=isSoundOn?'サウンド: ON':'サウンド: OFF'}}
const nameInput=page.querySelector('#player-name-input');if(nameInput){const currentName=(window.FloppyState&&window.FloppyState.playerName)||'ゆきんこ';nameInput.value=currentName;nameInput.addEventListener('input',function(e){const val=e.target.value.trim()||'ゆきんこ';window.FloppyState.playerName=val;try{localStorage.setItem('floppy_player_name',val)}catch(err){}
if(window._updatePreviewNameTag){window._updatePreviewNameTag(val)}})}
const currentColor=(window.FloppyState&&window.FloppyState.playerColor)||'#38bdf8';const colorBtns=page.querySelectorAll('.color-chip');colorBtns.forEach(function(btn){const col=btn.getAttribute('data-color');if(col===currentColor){btn.classList.add('active')}else{btn.classList.remove('active')}
btn.addEventListener('click',function(e){colorBtns.forEach(function(b){b.classList.remove('active')});const target=e.currentTarget;target.classList.add('active');const selectedCol=target.getAttribute('data-color')||'#38bdf8';window.FloppyState.playerColor=selectedCol;try{localStorage.setItem('floppy_player_color',selectedCol)}catch(err){}
if(window._updatePreviewCharacterColor){window._updatePreviewCharacterColor(selectedCol)}})});window.addEventListener('beforeunload',function(){if(window.cleanupHostRoom)window.cleanupHostRoom();});window.addEventListener('pagehide',function(){if(window.cleanupHostRoom)window.cleanupHostRoom();});if(window.scanPublicSlots){window.scanPublicSlots(!0)}
try{if(!window._floppyLobbyChannel){window._floppyLobbyChannel=new BroadcastChannel('floppy_room_channel');window._floppyLobbyChannel.onmessage=function(ev){const data=ev.data;if(data&&(data.type==='ROOM_ANNOUNCE'||data.type==='ROOM_UPDATE'||data.type==='ROOM_CLOSED')){if(window.scanPublicSlots)window.scanPublicSlots(!1);}}}}catch(e){}
window.addEventListener('storage',function(e){if(e.key==='floppy_active_public_rooms'){if(window.scanPublicSlots)window.scanPublicSlots(!1);}});if(window._lobbyPollTimer)clearInterval(window._lobbyPollTimer);window._lobbyPollTimer=setInterval(function(){const nav=document.querySelector('#appNavigator');if(nav&&nav.topPage&&nav.topPage.id==='home-page'){if(window.scanPublicSlots)window.scanPublicSlots(!1);}},4000)}
window.initHomePage=initHomePage;function scanPublicSlots(isInitial){if(window._isScanningSlots)return;const container=document.querySelector('#rooms-list-container');if(!container)return;const refreshIcon=document.querySelector('#refresh-rooms-btn i');if(refreshIcon)refreshIcon.classList.add('fa-spin');window._isScanningSlots=!0;const discoveredMap={};const now=Date.now();try{const sharedRoomsJson=localStorage.getItem('floppy_active_public_rooms');if(sharedRoomsJson){const parsed=JSON.parse(sharedRoomsJson);Object.keys(parsed).forEach(function(k){const r=parsed[k];if(r&&(now-(r.lastHeartbeat||0))<10000&&!r.isPlaying){discoveredMap[k]=r}})}}catch(e){}
const nav=document.querySelector('#appNavigator');const isCurrentlyInRoomPage=nav&&nav.topPage&&nav.topPage.id==='room-page';if(isCurrentlyInRoomPage&&window.FloppyState&&window.FloppyState.activeRoom&&window.FloppyState.activeRoom.roomType==='public'&&!window.FloppyState.activeRoom.isPlaying){const myRoom=window.FloppyState.activeRoom;discoveredMap[myRoom.slotId]={slotId:myRoom.slotId,hostName:myRoom.hostName||window.FloppyState.playerName,stageId:myRoom.stageId||'lake',maxPlayers:myRoom.maxPlayers||4,currentPlayers:(myRoom.players&&myRoom.players.length)?myRoom.players.length:1,matchTimeSec:myRoom.matchTimeSec||120,isPlaying:!1,lastHeartbeat:now}}
const currentEntries=container.querySelectorAll('.room-entry-item');if(isInitial&&currentEntries.length===0&&Object.keys(discoveredMap).length===0){container.innerHTML='<div class="loading-rooms-indicator">'+'<i class="fa-solid fa-circle-notch fa-spin"></i> <span>オンラインルーム探索中 (P2P)...</span>'+'</div>'}
const slotCount=4;let scannerPeer=null;try{if(typeof Peer!=='undefined'){const tempId='floppy-scan-'+Math.random().toString(36).substring(2,7);scannerPeer=new Peer(tempId,{debug:0,config:{iceServers:[{urls:'stun:stun.l.google.com:19302'}]}})}}catch(e){}
let finishedQueries=0;let isScanCompleted=!1;function finishScan(){if(isScanCompleted)return;isScanCompleted=!0;window._isScanningSlots=!1;if(refreshIcon)refreshIcon.classList.remove('fa-spin');window._cachedDiscoveredRooms=discoveredMap;renderRoomsList(Object.values(discoveredMap));if(scannerPeer){setTimeout(function(){try{scannerPeer.destroy()}catch(err){}},100)}}
function checkDone(){finishedQueries++;if(finishedQueries>=slotCount){finishScan()}}
if(scannerPeer){const scanTimeout=setTimeout(function(){finishScan()},1200);scannerPeer.on('open',function(){for(let i=1;i<=slotCount;i++){const targetSlot='floppy-pub-slot-0'+i;if(isCurrentlyInRoomPage&&window.FloppyState.activeRoom&&window.FloppyState.activeRoom.slotId===targetSlot){checkDone();continue}
try{const conn=scannerPeer.connect(targetSlot,{reliable:!0});let responded=!1;const timer=setTimeout(function(){if(!responded){try{conn.close()}catch(e){}
checkDone()}},600);conn.on('open',function(){conn.send({t:0,s:scannerPeer.id,d:{query:'status'}})});conn.on('data',function(data){if(data&&(data.t===1||data.type==='ROOM_STATUS')){responded=!0;clearTimeout(timer);const rData=data.d||data;if(!rData.isPlaying){discoveredMap[targetSlot]={slotId:targetSlot,hostName:rData.hostName||'ホスト',stageId:rData.stageId||'lake',maxPlayers:rData.maxPlayers||4,currentPlayers:rData.currentPlayers||1,matchTimeSec:rData.matchTimeSec||120,isPlaying:!1,lastHeartbeat:Date.now()}}else{delete discoveredMap[targetSlot]}
try{conn.close()}catch(e){}
checkDone()}});conn.on('error',function(){clearTimeout(timer);checkDone()})}catch(e){checkDone()}}});scannerPeer.on('error',function(){clearTimeout(scanTimeout);finishScan()})}else{finishScan()}
function renderRoomsList(rooms){const target=document.querySelector('#rooms-list-container');if(!target)return;const availableRooms=(rooms||[]).filter(function(r){return r&&!r.isPlaying});if(availableRooms.length===0){target.innerHTML='<div class="empty-rooms-msg">'+'<i class="fa-solid fa-cloud-moon" style="font-size: 1.6rem; color: #64748b; margin-bottom: 8px; display: block;"></i>'+'現在、募集中の公開ルームはありません。<br>'+'<span style="font-size: 0.78rem; color: #94a3b8;">「クイックマッチ」または「部屋を作成する」からすぐに対戦できます！</span>'+'</div>';return}
let html='';availableRooms.forEach(function(room){const stageName=room.stageId==='cliff'?'雪山クリフ':'こおりの湖';const statusBadge='<span style="color:#10b981;">● 募集中</span>';const curPlayers=room.currentPlayers||1;const isFull=curPlayers>=(room.maxPlayers||4);const btnLabel=isFull?'満員':'参加';const btnDisabled=isFull?'disabled style="opacity:0.5; cursor:not-allowed;"':'';html+='<div class="room-entry-item">'+'<div class="room-info-left">'+'<div class="room-host-title"><i class="fa-solid fa-snowflake"></i> '+escapeHtml(room.hostName)+' の部屋</div>'+'<div class="room-tags-row">'+'<span><i class="fa-solid fa-map"></i> '+stageName+'</span>'+'<span><i class="fa-solid fa-users"></i> '+curPlayers+'/'+room.maxPlayers+'</span>'+'<span>'+statusBadge+'</span>'+'</div>'+'</div>'+'<button class="join-room-btn join-slot-btn" '+btnDisabled+' data-slot="'+room.slotId+'" data-stage="'+(room.stageId||'lake')+'" data-max="'+(room.maxPlayers||4)+'" data-host="'+escapeHtml(room.hostName)+'">'+btnLabel+'</button>'+'</div>'});target.innerHTML=html;target.querySelectorAll('.join-slot-btn:not([disabled])').forEach(function(btn){btn.addEventListener('click',function(e){const slotId=e.currentTarget.getAttribute('data-slot');const stageId=e.currentTarget.getAttribute('data-stage')||'lake';const maxPlayers=parseInt(e.currentTarget.getAttribute('data-max')||'4',10);const hostName=e.currentTarget.getAttribute('data-host')||'ホスト';if(window.joinRoomDirect){window.joinRoomDirect({slotId:slotId,stageId:stageId,maxPlayers:maxPlayers,hostName:hostName,roomType:'public'})}})})}
function escapeHtml(str){if(!str)return'';return str.replace(/[&<>"']/g,function(tag){return({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[tag]||tag})}}
window.scanPublicSlots=scanPublicSlots;function handleQuickMatch(){if(window._isQuickMatching)return;window._isQuickMatching=!0;const qmModal=document.querySelector('#quick-match-modal');const statusTitle=document.querySelector('#qm-status-title');const statusDesc=document.querySelector('#qm-status-desc');if(qmModal)qmModal.classList.remove('hidden');if(statusTitle)statusTitle.textContent='対戦相手を探索中...';if(statusDesc)statusDesc.innerHTML='<span>他プレイヤーの募集中ルームを探しています...</span>';if(window.FloppySound&&window.FloppySound.playWhistle){window.FloppySound.playWhistle()}
if(window.cleanupHostRoom){window.cleanupHostRoom()}
const myName=(window.FloppyState&&window.FloppyState.playerName)||'ゆきんこ';const myColor=(window.FloppyState&&window.FloppyState.playerColor)||'#38bdf8';let isMatched=!1;let isCancelled=!1;let qmPeer=null;let searchLoopTimer=null;let loopCount=0;function stopSearch(){isCancelled=!0;if(searchLoopTimer)clearTimeout(searchLoopTimer);searchLoopTimer=null;window._isQuickMatching=!1;if(qmModal)qmModal.classList.add('hidden');if(qmPeer){try{qmPeer.destroy()}catch(e){}
qmPeer=null}}
const cancelBtn=document.querySelector('#qm-cancel-btn');if(cancelBtn){cancelBtn.onclick=function(){stopSearch()}}
const forceHostBtn=document.querySelector('#qm-force-host-btn');if(forceHostBtn){forceHostBtn.onclick=function(){isMatched=!0;stopSearch();if(window.createAndHostRoom){window.createAndHostRoom({roomType:'public',stageId:'lake',maxPlayers:4,matchTimeSec:120,allowLateJoin:!1},1)}}}
try{if(typeof Peer!=='undefined'){qmPeer=new Peer(null,{debug:0,config:{iceServers:[{urls:'stun:stun.l.google.com:19302'}]}})}}catch(e){}
if(!qmPeer){if(statusDesc)statusDesc.innerHTML='<span style="color:#f43f5e;">通信基盤の起動に失敗しました。「自分で部屋を作成」をお試しください。</span>';return}
qmPeer.on('open',function(myId){if(isCancelled)return;window.FloppyState.myPeerId=myId;window.FloppyState.peer=qmPeer;function executeSearchPass(){if(isMatched||isCancelled)return;loopCount++;if(statusDesc){statusDesc.innerHTML='<span>他プレイヤーの募集中ルームを探しています (探索中: '+loopCount+'回目)...</span>'}
const now=Date.now();try{const shared=JSON.parse(localStorage.getItem('floppy_active_public_rooms')||'{}');const validRooms=Object.values(shared).filter(function(r){return r&&(now-(r.lastHeartbeat||0))<10000&&!r.isPlaying&&(r.currentPlayers||1)<(r.maxPlayers||4)});if(validRooms.length>0){const localTarget=validRooms[0];isMatched=!0;if(statusTitle)statusTitle.textContent='対戦部屋を発見！接続中...';setTimeout(function(){stopSearch();if(window.joinRoomDirect){window.joinRoomDirect({slotId:localTarget.slotId,stageId:localTarget.stageId||'lake',maxPlayers:localTarget.maxPlayers||4,hostName:localTarget.hostName||'ホスト',roomType:'public'})}},200);return}}catch(e){}
const slotsToTry=['floppy-pub-slot-01','floppy-pub-slot-02','floppy-pub-slot-03','floppy-pub-slot-04'];slotsToTry.forEach(function(targetSlot){if(isMatched||isCancelled)return;try{const conn=qmPeer.connect(targetSlot,{reliable:!0});const passTimer=setTimeout(function(){try{conn.close()}catch(err){}},2200);conn.on('open',function(){clearTimeout(passTimer);if(isMatched||isCancelled){try{conn.close()}catch(e){}
return}
conn.send({t:2,s:myId,type:'JOIN_REQUEST',playerName:myName,playerColor:myColor,passcode:''})});conn.on('data',function(data){if(!data||isMatched||isCancelled)return;if(data.t===3||data.type==='JOIN_RESPONSE'){const res=data.d||data;if(res.accepted&&!isMatched){isMatched=!0;clearTimeout(passTimer);if(searchLoopTimer)clearTimeout(searchLoopTimer);if(statusTitle)statusTitle.textContent='部屋に入室しました！';window._isQuickMatching=!1;window.FloppyState.isHost=!1;window.FloppyState.isBotMatch=!1;window.FloppyState.hostConn=conn;window.FloppyState.activeRoom=res.room||{slotId:targetSlot,hostName:'ホスト',stageId:'lake',maxPlayers:4,players:[]};conn.on('data',function(msg){if(!msg)return;if(msg.type==='ROOM_UPDATE'){window.FloppyState.activeRoom=msg.room;const curPage=document.querySelector('#room-page');if(curPage&&window.initRoomPage)window.initRoomPage({target:curPage})}else if(msg.type==='START_GAME'){window.FloppyState.currentGameSettings=msg.settings||{stageId:msg.room?msg.room.stageId:'lake',matchTimeSec:msg.room?msg.room.matchTimeSec:120,players:msg.room?msg.room.players:[]};if(window.FloppySound)window.FloppySound.playWhistle();const appNav=document.querySelector('#appNavigator');if(appNav&&appNav.topPage&&appNav.topPage.id==='room-page'){appNav.pushPage('game.html')}}else if(msg.type==='CHAT'){if(msg.sender!==myName){if(window.appendRoomChatMsg)window.appendRoomChatMsg(msg.sender,msg.text);}}});setTimeout(function(){if(qmModal)qmModal.classList.add('hidden');const nav=document.querySelector('#appNavigator');if(nav)nav.pushPage('room.html');},200)}}});conn.on('error',function(){clearTimeout(passTimer)})}catch(err){}});if(!isMatched&&!isCancelled){searchLoopTimer=setTimeout(executeSearchPass,2000)}}
executeSearchPass()});qmPeer.on('error',function(){if(!isCancelled&&!isMatched){searchLoopTimer=setTimeout(function(){if(!isCancelled&&!isMatched)handleQuickMatch();},2000)}})}
window.handleQuickMatch=handleQuickMatch;function handleOpenCreateModal(){const modal=document.querySelector('#create-room-modal');if(modal)modal.classList.remove('hidden');const typeSelect=document.querySelector('#create-room-type');const passcodeGroup=document.querySelector('#passcode-form-group');if(typeSelect&&passcodeGroup){typeSelect.addEventListener('change',function(){if(typeSelect.value==='private'){passcodeGroup.classList.remove('hidden')}else{passcodeGroup.classList.add('hidden')}})}}
window.handleOpenCreateModal=handleOpenCreateModal;function handleCloseCreateModal(){const modal=document.querySelector('#create-room-modal');if(modal)modal.classList.add('hidden');}
window.handleCloseCreateModal=handleCloseCreateModal;function handleCreateRoomSubmit(){const modal=document.querySelector('#create-room-modal');if(modal)modal.classList.add('hidden');const roomType=document.querySelector('#create-room-type').value||'public';const passcode=(document.querySelector('#create-passcode-input').value||'').trim();const stageId=document.querySelector('#create-stage-select').value||'lake';const maxPlayers=parseInt(document.querySelector('#create-max-players').value||'4',10);const matchTimeSec=parseInt(document.querySelector('#create-time-limit').value||'120',10);const allowLateJoin=document.querySelector('#create-late-join').value==='true';let slotId='floppy-pub-slot-01';if(roomType==='private'){slotId='floppy-priv-'+(passcode||'secret')}else{const discovered=window._cachedDiscoveredRooms||{};for(let i=1;i<=6;i++){const testSlot='floppy-pub-slot-'+(i<10?'0'+i:i);if(!discovered[testSlot]){slotId=testSlot;break}}}
if(window.createAndHostRoom){window.createAndHostRoom({slotId:slotId,roomType:roomType,passcode:passcode,stageId:stageId,maxPlayers:maxPlayers,matchTimeSec:matchTimeSec,allowLateJoin:allowLateJoin})}}
window.handleCreateRoomSubmit=handleCreateRoomSubmit;function handleOpenPasscodeModal(){const modal=document.querySelector('#passcode-join-modal');if(modal)modal.classList.remove('hidden');}
window.handleOpenPasscodeModal=handleOpenPasscodeModal;function handleClosePasscodeModal(){const modal=document.querySelector('#passcode-join-modal');if(modal)modal.classList.add('hidden');}
window.handleClosePasscodeModal=handleClosePasscodeModal;function handleJoinWithPasscode(){const input=document.querySelector('#join-passcode-input');const feedback=document.querySelector('#join-status-feedback');const passcode=input?input.value.trim():'';if(!passcode){if(feedback)feedback.innerHTML='<span style="color:#f43f5e;">合言葉を入力してください</span>';return}
const modal=document.querySelector('#passcode-join-modal');if(modal)modal.classList.add('hidden');window.joinRoomDirect({slotId:'floppy-priv-'+passcode,stageId:'lake',maxPlayers:4,hostName:'合言葉ホスト',roomType:'private',passcode:passcode})}
window.handleJoinWithPasscode=handleJoinWithPasscode;function joinRoomDirect(roomConfig){const nav=document.querySelector('#appNavigator');if(!nav)return;const hostName=(roomConfig&&roomConfig.hostName)?roomConfig.hostName:'ホスト';const myName=(window.FloppyState&&window.FloppyState.playerName)||'ゆきんこ';const myColor=(window.FloppyState&&window.FloppyState.playerColor)||'#38bdf8';window.FloppyState.isHost=!1;window.FloppyState.isBotMatch=!1;window.FloppyState.activeRoom={slotId:roomConfig.slotId,hostName:hostName,stageId:roomConfig.stageId||'lake',maxPlayers:roomConfig.maxPlayers||4,matchTimeSec:120,roomType:roomConfig.roomType||'public',passcode:roomConfig.passcode||'',isPlaying:!1,players:[{id:'host-1',name:hostName,color:'#f43f5e',isHost:!0,isReady:!0},{id:'me',name:myName,color:myColor,isHost:!1,isReady:!1}]};let isConnectedToHost=!1;if(typeof Peer!=='undefined'){try{const guestPeer=new Peer(null,{debug:0,config:{iceServers:[{urls:'stun:stun.l.google.com:19302'}]}});guestPeer.on('open',function(id){window.FloppyState.myPeerId=id;const conn=guestPeer.connect(roomConfig.slotId,{reliable:!0});window.FloppyState.hostConn=conn;const connTimeout=setTimeout(function(){if(!isConnectedToHost){try{conn.close()}catch(e){}
try{guestPeer.destroy()}catch(e){}
if(window.cleanupHostRoom)window.cleanupHostRoom();const appNav=document.querySelector('#appNavigator');if(appNav&&appNav.topPage&&appNav.topPage.id==='room-page'){appNav.popPage()}}},2500);conn.on('open',function(){isConnectedToHost=!0;clearTimeout(connTimeout);conn.send({t:2,s:id,type:'JOIN_REQUEST',playerName:myName,playerColor:myColor,passcode:roomConfig.passcode||''})});conn.on('data',function(data){if(!data)return;if(data.t===3||data.type==='JOIN_RESPONSE'){const res=data.d||data;if(res.accepted&&res.room){window.FloppyState.activeRoom=res.room;const curPage=document.querySelector('#room-page');if(curPage&&window.initRoomPage)window.initRoomPage({target:curPage})}else{try{conn.close()}catch(e){}
if(window.cleanupHostRoom)window.cleanupHostRoom();const appNav=document.querySelector('#appNavigator');if(appNav&&appNav.topPage&&appNav.topPage.id==='room-page'){appNav.popPage()}}}else if(data.type==='ROOM_UPDATE'){window.FloppyState.activeRoom=data.room;const curPage=document.querySelector('#room-page');if(curPage&&window.initRoomPage)window.initRoomPage({target:curPage})}else if(data.type==='START_GAME'){window.FloppyState.currentGameSettings=data.settings||{stageId:data.room?data.room.stageId:'lake',matchTimeSec:data.room?data.room.matchTimeSec:120,players:data.room?data.room.players:[]};if(window.FloppySound)window.FloppySound.playWhistle();const appNav=document.querySelector('#appNavigator');if(appNav&&appNav.topPage&&appNav.topPage.id==='room-page'){appNav.pushPage('game.html')}}else if(data.type==='CHAT'){if(data.s&&data.s===window.FloppyState.myPeerId)return;if(data.sender===myName)return;if(window.appendRoomChatMsg)window.appendRoomChatMsg(data.sender,data.text);}});conn.on('error',function(){clearTimeout(connTimeout);if(!isConnectedToHost){if(window.cleanupHostRoom)window.cleanupHostRoom();const appNav=document.querySelector('#appNavigator');if(appNav&&appNav.topPage&&appNav.topPage.id==='room-page'){appNav.popPage()}}})});window.FloppyState.peer=guestPeer}catch(e){}}
nav.pushPage('room.html')}
window.joinRoomDirect=joinRoomDirect;function handleStartBotGame(){const nav=document.querySelector('#appNavigator');if(!nav)return;if(window.FloppySound)window.FloppySound.playWhistle();window.FloppyState.isHost=!0;window.FloppyState.isBotMatch=!0;window.FloppyState.currentGameSettings={stageId:'lake',matchTimeSec:120,players:[{id:'p0',name:window.FloppyState.playerName+' (あなた)',color:window.FloppyState.playerColor,isBot:!1},{id:'p1',name:'Bot トム',color:'#f43f5e',isBot:!0},{id:'p2',name:'Bot ルーカス',color:'#10b981',isBot:!0},{id:'p3',name:'Bot エマ',color:'#fbbf24',isBot:!0}]};nav.pushPage('game.html')}
window.handleStartBotGame=handleStartBotGame;function handleOpenHelpModal(){const m=document.querySelector('#help-modal');if(m)m.classList.remove('hidden');}
window.handleOpenHelpModal=handleOpenHelpModal;function handleCloseHelpModal(){const m=document.querySelector('#help-modal');if(m)m.classList.add('hidden');}
window.handleCloseHelpModal=handleCloseHelpModal;function handleSoundToggle(event){if(event){event.preventDefault();event.stopPropagation()}
const now=Date.now();if(window._lastSoundToggleTime&&(now-window._lastSoundToggleTime)<220){return}
window._lastSoundToggleTime=now;if(!window.FloppySound&&window.initSoundEngine){window.initSoundEngine()}
if(!window.FloppySound)return;const isNowOn=window.FloppySound.toggle();const icon=document.querySelector('#sound-icon');const textEl=document.querySelector('#sound-toggle-text');const btn=document.querySelector('#sound-toggle-btn');if(icon){icon.className=isNowOn?'fa-solid fa-volume-high':'fa-solid fa-volume-xmark'}
if(textEl){textEl.textContent=isNowOn?'サウンド: ON':'サウンド: OFF'}else if(btn){btn.innerHTML='<i class="fa-solid '+(isNowOn?'fa-volume-high':'fa-volume-xmark')+'" id="sound-icon"></i><span id="sound-toggle-text">サウンド: '+(isNowOn?'ON':'OFF')+'</span>'}
if(isNowOn&&window.FloppySound.playGetUp){window.FloppySound.playGetUp()}}
window.handleSoundToggle=handleSoundToggle;function initRoomPage(event){const page=event.target||document.querySelector('#room-page');if(!page)return;const room=window.FloppyState.activeRoom||{slotId:'floppy-pub-slot-01',hostName:window.FloppyState.playerName,stageId:'lake',maxPlayers:4,matchTimeSec:120,roomType:'public',players:[{id:'me',name:window.FloppyState.playerName,color:window.FloppyState.playerColor,isHost:!0,isReady:!0}]};const typeTag=page.querySelector('#room-type-tag');if(typeTag){if(room.roomType==='private'){typeTag.className='badge-tag private-badge';typeTag.innerHTML='<i class="fa-solid fa-lock"></i> 非公開ルーム'}else{typeTag.className='badge-tag public-badge';typeTag.innerHTML='<i class="fa-solid fa-globe"></i> 公開ルーム'}}
const titleText=page.querySelector('#room-title-text');if(titleText)titleText.textContent=room.hostName+' のルーム';const idDisplay=page.querySelector('#room-id-display');if(idDisplay)idDisplay.textContent='Slot: '+room.slotId;const stageLabel=page.querySelector('#room-stage-label');if(stageLabel)stageLabel.textContent=room.stageId==='cliff'?'雪山クリフ':'こおりの湖';const timeLimitLabel=page.querySelector('#room-time-limit-label');if(timeLimitLabel)timeLimitLabel.textContent=room.matchTimeSec===0?'無制限':(room.matchTimeSec+'秒');const passcodePill=page.querySelector('#room-passcode-pill');const passcodeLabel=page.querySelector('#room-passcode-label');if(passcodePill&&passcodeLabel){if(room.roomType==='private'&&room.passcode){passcodePill.style.display='inline-block';passcodeLabel.textContent=room.passcode}else{passcodePill.style.display='none'}}
const startBtn=page.querySelector('#room-start-btn');const readyBtn=page.querySelector('#room-ready-toggle-btn');if(window.FloppyState.isHost){if(startBtn)startBtn.classList.remove('hidden');if(readyBtn)readyBtn.classList.add('hidden');}else{if(startBtn)startBtn.classList.add('hidden');if(readyBtn)readyBtn.classList.remove('hidden');}
renderRoster(page,room);if(window._roomHeartbeatTimer)clearInterval(window._roomHeartbeatTimer);if(window.FloppyState.isHost&&room.roomType==='public'){const sendHeartbeat=function(){try{const roomsMap=JSON.parse(localStorage.getItem('floppy_active_public_rooms')||'{}');roomsMap[room.slotId]={slotId:room.slotId,hostName:room.hostName||window.FloppyState.playerName,hostColor:window.FloppyState.playerColor,stageId:room.stageId||'lake',maxPlayers:room.maxPlayers||4,currentPlayers:(room.players&&room.players.length)?room.players.length:1,matchTimeSec:room.matchTimeSec||120,isPlaying:!1,lastHeartbeat:Date.now()};localStorage.setItem('floppy_active_public_rooms',JSON.stringify(roomsMap));if(window._floppyRoomChannel){window._floppyRoomChannel.postMessage({type:'ROOM_ANNOUNCE',room:roomsMap[room.slotId]})}}catch(e){}};sendHeartbeat();window._roomHeartbeatTimer=setInterval(sendHeartbeat,2000)}
try{if(!window._floppyRoomChannel){window._floppyRoomChannel=new BroadcastChannel('floppy_room_channel')}
window._floppyRoomChannel.onmessage=function(ev){const data=ev.data;if(!data)return;if(data.type==='START_GAME'){const nav=document.querySelector('#appNavigator');if(nav&&nav.topPage&&nav.topPage.id==='room-page'){window.FloppyState.currentGameSettings=data.settings||{stageId:data.room?data.room.stageId:'lake',matchTimeSec:data.room?data.room.matchTimeSec:120,players:data.room?data.room.players:[]};if(window.FloppySound)window.FloppySound.playWhistle();nav.pushPage('game.html')}}else if(data.type==='ROOM_UPDATE'){if(window.FloppyState.activeRoom&&!window.FloppyState.isHost){window.FloppyState.activeRoom=data.room;const curPage=document.querySelector('#room-page');if(curPage)renderRoster(curPage,data.room);}}else if(data.type==='CHAT'){if(data.sender!==(window.FloppyState&&window.FloppyState.playerName)){if(window.appendRoomChatMsg)window.appendRoomChatMsg(data.sender,data.text);}}}}catch(e){}
function renderRoster(p,r){const roster=p.querySelector('#players-roster-container');const countLabel=p.querySelector('#room-player-count-label');if(!roster)return;const players=r.players||[];if(countLabel)countLabel.textContent=players.length+' / '+r.maxPlayers;let html='';players.forEach(function(pl){let statusClass='status-waiting';let statusText='待機中';if(pl.isHost){statusClass='status-host';statusText='ホスト'}else if(pl.isReady){statusClass='status-ready';statusText='準備完了'}
html+='<div class="player-slot-card">'+'<div class="player-avatar-circle" style="background-color: '+(pl.color||'#38bdf8')+';"><i class="fa-solid fa-person-snowboarding"></i></div>'+'<div class="player-slot-name">'+pl.name+'</div>'+'<div class="player-slot-status '+statusClass+'">'+statusText+'</div>'+'</div>'});const emptySlots=(r.maxPlayers||4)-players.length;for(let i=0;i<emptySlots;i++){html+='<div class="empty-slot-card"><i class="fa-solid fa-user-plus"></i><span>空きスロット</span></div>'}
roster.innerHTML=html}}
window.initRoomPage=initRoomPage;function handleRoomAddBot(){const room=window.FloppyState.activeRoom;if(!room)return;if(room.players.length>=room.maxPlayers)return;const botColors=['#f43f5e','#10b981','#fbbf24','#a855f7'];const botNames=['Bot トム','Bot ルーカス','Bot エマ','Bot ノア'];const idx=room.players.length;const newBot={id:'bot-'+Date.now()+'-'+idx,name:botNames[idx%botNames.length],color:botColors[idx%botColors.length],isHost:!1,isReady:!0,isBot:!0};room.players.push(newBot);const page=document.querySelector('#room-page');if(page&&window.initRoomPage){window.initRoomPage({target:page})}
try{if(window._floppyRoomChannel){window._floppyRoomChannel.postMessage({type:'ROOM_UPDATE',room:room})}}catch(e){}
if(window.checkAndAutoStartIfReady){window.checkAndAutoStartIfReady(room)}}
window.handleRoomAddBot=handleRoomAddBot;function handleRoomToggleReady(){const btn=document.querySelector('#room-ready-toggle-btn');const room=window.FloppyState.activeRoom;if(!room||!btn)return;const myPlayer=room.players.find(function(p){return p.id==='me'||(!p.isHost&&!p.isBot)});if(myPlayer){myPlayer.isReady=!myPlayer.isReady;if(myPlayer.isReady){btn.innerHTML='<i class="fa-solid fa-circle-xmark"></i> 準備解除';btn.style.background='#64748b'}else{btn.innerHTML='<i class="fa-solid fa-circle-check"></i> 準備完了';btn.style.background='linear-gradient(135deg, #10b981 0%, #059669 100%)'}
const page=document.querySelector('#room-page');if(page&&window.initRoomPage)window.initRoomPage({target:page});if(window.FloppyState.hostConn){try{window.FloppyState.hostConn.send({type:'TOGGLE_READY'})}catch(e){}}}}
window.handleRoomToggleReady=handleRoomToggleReady;function handleRoomStartGame(){const nav=document.querySelector('#appNavigator');const room=window.FloppyState.activeRoom;if(!room||!nav)return;if(nav.topPage&&nav.topPage.id==='game-page')return;room.isPlaying=!0;if(window._roomHeartbeatTimer){clearInterval(window._roomHeartbeatTimer);window._roomHeartbeatTimer=null}
try{const roomsMap=JSON.parse(localStorage.getItem('floppy_active_public_rooms')||'{}');if(roomsMap[room.slotId]){delete roomsMap[room.slotId];localStorage.setItem('floppy_active_public_rooms',JSON.stringify(roomsMap))}}catch(e){}
try{if(window._floppyRoomChannel){window._floppyRoomChannel.postMessage({type:'ROOM_CLOSED',slotId:room.slotId})}}catch(e){}
const fallbackBotNames=['Bot トム','Bot ルーカス','Bot エマ','Bot ノア'];while(room.players.length<2){const bIdx=room.players.length;room.players.push({id:'bot-auto-'+(bIdx+1),name:fallbackBotNames[bIdx%fallbackBotNames.length],color:'#f43f5e',isHost:!1,isReady:!0,isBot:!0})}
window.FloppyState.currentGameSettings={stageId:room.stageId||'lake',matchTimeSec:room.matchTimeSec||120,players:room.players};if(window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(c){try{c.send({type:'START_GAME',room:room,settings:window.FloppyState.currentGameSettings})}catch(err){}})}
try{if(window._floppyRoomChannel){window._floppyRoomChannel.postMessage({type:'START_GAME',room:room,settings:window.FloppyState.currentGameSettings})}}catch(e){}
if(window.FloppySound)window.FloppySound.playWhistle();nav.pushPage('game.html')}
window.handleRoomStartGame=handleRoomStartGame;function handleRoomLeave(){if(window.cleanupHostRoom){window.cleanupHostRoom()}
const nav=document.querySelector('#appNavigator');if(nav)nav.popPage();}
window.handleRoomLeave=handleRoomLeave;function initGamePage(event){const page=event.target||document.querySelector('#game-page');if(!page)return;const container=page.querySelector('#three-canvas-container');if(!container)return;if(typeof THREE==='undefined'){console.error('Three.js is not loaded');return}
const settings=window.FloppyState.currentGameSettings||{stageId:'lake',matchTimeSec:120,players:[{id:'p0',name:window.FloppyState.playerName||'ゆきんこ',color:window.FloppyState.playerColor||'#38bdf8',isBot:!1,isHost:!0},{id:'p1',name:'Bot トム',color:'#f43f5e',isBot:!0}]};const stageNameEl=page.querySelector('#hud-stage-name');if(stageNameEl){stageNameEl.textContent=settings.stageId==='cliff'?'🏔️ 雪山クリフ (広大アリーナ)':'❄️ こおりの湖 (広大アリーナ)'}
const width=Math.max(container.clientWidth||window.innerWidth,200);const height=Math.max(container.clientHeight||window.innerHeight,200);const scene=new THREE.Scene();scene.background=new THREE.Color(settings.stageId==='cliff'?0x0f172a:0x082f49);scene.fog=new THREE.FogExp2(settings.stageId==='cliff'?0x0f172a:0x082f49,0.012);const camera=new THREE.PerspectiveCamera(60,width/height,0.1,400);camera.position.set(0,8,20);const renderer=new THREE.WebGLRenderer({antialias:!0,alpha:!1,powerPreference:'high-performance'});renderer.setSize(width,height);renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));renderer.shadowMap.enabled=!0;renderer.shadowMap.type=THREE.PCFSoftShadowMap;container.innerHTML='';container.appendChild(renderer.domElement);const hemiLight=new THREE.HemisphereLight(0xffffff,0x38bdf8,0.75);scene.add(hemiLight);const dirLight=new THREE.DirectionalLight(0xfffbeb,1.0);dirLight.position.set(40,70,40);dirLight.castShadow=!0;dirLight.shadow.mapSize.width=1024;dirLight.shadow.mapSize.height=1024;dirLight.shadow.camera.near=0.5;dirLight.shadow.camera.far=200;const d=55;dirLight.shadow.camera.left=-d;dirLight.shadow.camera.right=d;dirLight.shadow.camera.top=d;dirLight.shadow.camera.bottom=-d;scene.add(dirLight);const terrainColliders=[];buildStageTerrain(scene,settings.stageId,terrainColliders);let myPlayerIdx=0;if(!window.FloppyState.isHost){const foundIdx=settings.players.findIndex(function(p){return!p.isHost&&!p.isBot&&(p.id==='me'||p.name===window.FloppyState.playerName)});if(foundIdx!==-1){myPlayerIdx=foundIdx}else{myPlayerIdx=1}}
const gamePlayers=[];const spawnRadius=20;const numPlayers=settings.players.length;settings.players.forEach(function(pData,idx){const angle=(idx/numPlayers)*Math.PI*2;const spawnX=Math.cos(angle)*spawnRadius;const spawnZ=Math.sin(angle)*spawnRadius;const pObj=createFloppyCharacter(scene,pData,spawnX,1.0,spawnZ);pObj.isMe=(idx===myPlayerIdx);pObj.playerIndex=idx;pObj.spawnX=spawnX;pObj.spawnZ=spawnZ;pObj.targetPos=new THREE.Vector3(spawnX,1.0,spawnZ);pObj.targetRotY=0;pObj.invincibleTimer=0;pObj.isDiving=!1;pObj.diveTimer=0;pObj.diveCooldown=0;gamePlayers.push(pObj)});const myPlayerObj=gamePlayers[myPlayerIdx]||gamePlayers[0];page._getMyPlayer=function(){return myPlayerObj};let snowballs=[];const particles=createParticleSystem(scene);const confettiSystem=createConfettiSystem(scene);const healItems=[];spawnHealItem(scene,healItems,0,1.0,10);spawnHealItem(scene,healItems,12,1.0,-12);spawnHealItem(scene,healItems,-12,1.0,12);const inputState={moveX:0,moveZ:0,cameraYaw:(myPlayerIdx*Math.PI),cameraPitch:0.28,isCharging:!1,chargeStart:0,chargeRate:0,keys:{}};function broadcastP2P(msg){if(window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){try{conn.send(msg)}catch(e){}})}else if(window.FloppyState.hostConn){try{window.FloppyState.hostConn.send(msg)}catch(e){}}}
let gameStarted=!1;let isGameFinishing=!1;let isSpectating=!1;let spectateTargetIdx=(myPlayerIdx===0?1:0);let timeScale=1.0;let matchWinner=null;let finishCamAngle=0;let matchTimeRemaining=settings.matchTimeSec||120;let countInterval=null;let isRunning=!0;let lastTime=performance.now();let p2pSyncAccumulator=0;function triggerDramaticFinish(reason,winnerIdx){if(isGameFinishing)return;isGameFinishing=!0;timeScale=0.25;if(winnerIdx!==undefined&&gamePlayers[winnerIdx]){matchWinner=gamePlayers[winnerIdx]}else{const alive=gamePlayers.filter(function(p){return!p.isDead});if(alive.length===1){matchWinner=alive[0]}else if(alive.length>1){const sortedAlive=alive.slice().sort(function(a,b){return(b.score||0)-(a.score||0)});matchWinner=sortedAlive[0]}else{const sorted=gamePlayers.slice().sort(function(a,b){return(b.score||0)-(a.score||0)});matchWinner=sorted[0]}}
if(window.FloppySound&&window.FloppySound.playFinishFanfare){window.FloppySound.playFinishFanfare()}
if(finishOverlay){if(finishTitleText)finishTitleText.textContent=(reason==='knockout'||reason==='ringout')?'K.O.!!':'FINISH!!';if(finishWinnerText)finishWinnerText.textContent=matchWinner?('WINNER: '+matchWinner.data.name+' 👑'):'MATCH COMPLETE';finishOverlay.classList.remove('hidden')}
if(matchWinner){spawnWinnerConfetti(confettiSystem,matchWinner.pos,150)}
setTimeout(function(){if(!isRunning)return;if(finishOverlay)finishOverlay.classList.add('hidden');endGame(page,gamePlayers);gameStarted=!1},2600)}
function handleIncomingP2PData(data,senderConnPeerId){if(!data)return;if(data.t===4||data.type==='SYNC_TRANSFORM'){const pIdx=data.idx!==undefined?data.idx:(window.FloppyState.isHost?1:0);const remoteP=gamePlayers[pIdx];if(remoteP&&!remoteP.isMe){if(data.p)remoteP.targetPos.set(data.p[0],data.p[1],data.p[2]);if(data.v)remoteP.vel.set(data.v[0],data.v[1],data.v[2]);if(data.r!==undefined)remoteP.targetRotY=data.r;if(data.score!==undefined)remoteP.score=data.score;if(data.hp!==undefined)remoteP.hp=data.hp;if(data.isDiving!==undefined)remoteP.isDiving=!!data.isDiving;if(data.isDead!==undefined&&data.isDead&&!remoteP.isDead){remoteP.isDead=!0;remoteP.hp=0;spawnHitParticles(particles,remoteP.pos,0xf43f5e,35)}
if(data.isDown!==undefined){remoteP.isDown=!!data.isDown;if(!remoteP.isDown)remoteP.downTimer=0}}
if(window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){if(conn.peer!==data.s&&conn.peer!==senderConnPeerId){try{conn.send(data)}catch(err){}}})}}else if(data.t===5||data.type==='THROW_SNOWBALL'){const pIdx=data.idx!==undefined?data.idx:(window.FloppyState.isHost?1:0);const thrower=gamePlayers[pIdx];if(thrower&&!thrower.isMe){spawnRemoteSnowball(scene,thrower,data,snowballs)}
if(window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){if(conn.peer!==data.s&&conn.peer!==senderConnPeerId){try{conn.send(data)}catch(err){}}})}}else if(data.t===6||data.type==='DAMAGE_EVENT'){const target=gamePlayers[data.targetIdx];const shooter=gamePlayers[data.shooterIdx];if(shooter&&data.shooterScore!==undefined){shooter.score=data.shooterScore}
if(target){target.hp=(data.targetHp!==undefined)?data.targetHp:Math.max(0,target.hp-(data.damage||20));target.isDown=!0;target.downTimer=1.4;target.invincibleTimer=0.35;if(data.impulse){target.vel.set(data.impulse[0],data.impulse[1],data.impulse[2])}
spawnHitParticles(particles,target.pos,0x38bdf8,16);if(window.FloppySound)window.FloppySound.playHit();if(target.isMe){const overlay=document.querySelector('#ragdoll-feedback-overlay');const textEl=document.querySelector('#ragdoll-banner-text');if(textEl)textEl.textContent='被弾！ふにゃ〜ん (HP: '+Math.max(0,Math.round(target.hp))+'%)';if(overlay)overlay.classList.remove('hidden');}}
if(window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){if(conn.peer!==data.s&&conn.peer!==senderConnPeerId){try{conn.send(data)}catch(err){}}})}}else if(data.t===8||data.type==='PLAYER_DEAD'){const deadP=gamePlayers[data.idx];if(deadP&&!deadP.isDead){deadP.isDead=!0;deadP.hp=0;deadP.mesh.rotation.z=Math.PI/2;spawnHitParticles(particles,deadP.pos,0xf43f5e,35);const desc=(data.reason==='ringout')?(deadP.data.name+' が転落！'):(deadP.data.name+' は力尽きた！');addKillFeed(page,'K.O.!!',desc)}
if(window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){if(conn.peer!==data.s&&conn.peer!==senderConnPeerId){try{conn.send(data)}catch(err){}}})}}else if(data.t===7||data.type==='FINISH_MATCH'){if(!isGameFinishing){triggerDramaticFinish(data.reason||'knockout',data.winnerIdx)}}else if(data.type==='RESULT_CHAT'||data.type==='CHAT'){const myId=window.FloppyState?(window.FloppyState.myPeerId||(window.FloppyState.peer&&window.FloppyState.peer.id)):'';const myName=window.FloppyState?window.FloppyState.playerName:'';const isFromMe=(data.s&&myId&&data.s===myId)||(data.sender===myName&&!window.FloppyState.isHost);if(!isFromMe){if(window.appendResultChatBubble){window.appendResultChatBubble(data.sender,data.text,!1)}}
if(window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){if(conn.peer!==data.s&&conn.peer!==senderConnPeerId){try{conn.send(data)}catch(err){}}})}}}
if(window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){conn.on('data',function(d){handleIncomingP2PData(d,conn.peer)})})}else if(window.FloppyState.hostConn){window.FloppyState.hostConn.on('data',function(d){handleIncomingP2PData(d,null)})}
try{if(window._floppyRoomChannel){window._floppyRoomChannel.onmessage=function(ev){const data=ev.data;if(data&&(data.type==='RESULT_CHAT'||data.type==='CHAT')){const myName=window.FloppyState?window.FloppyState.playerName:'';if(data.sender!==myName){if(window.appendResultChatBubble){window.appendResultChatBubble(data.sender,data.text,!1)}}}}}}catch(e){}
setupGameInputControls(page,inputState,function(){if(myPlayerObj&&!myPlayerObj.isDead&&gameStarted&&!isGameFinishing){triggerPlayerDive(myPlayerObj,inputState);broadcastP2P({t:4,idx:myPlayerIdx,isDiving:!0,p:[myPlayerObj.pos.x,myPlayerObj.pos.y,myPlayerObj.pos.z],v:[myPlayerObj.vel.x,myPlayerObj.vel.y,myPlayerObj.vel.z]})}},function(chargeTime){if(myPlayerObj&&!myPlayerObj.isDown&&!myPlayerObj.isDiving&&!myPlayerObj.isDead&&gameStarted&&!isGameFinishing){throwSnowball(scene,myPlayerObj,inputState,chargeTime,snowballs,function(throwPacket){throwPacket.idx=myPlayerIdx;broadcastP2P(throwPacket)})}});const countdownEl=page.querySelector('#countdown-overlay');const countNumEl=page.querySelector('#countdown-number');const finishOverlay=page.querySelector('#finish-impact-overlay');const finishTitleText=page.querySelector('#finish-title-text');const finishWinnerText=page.querySelector('#finish-winner-text');const timerDisplay=page.querySelector('#hud-timer-display');const hpFill=page.querySelector('#hud-hp-fill');const hpVal=page.querySelector('#hud-hp-val');const hpBox=page.querySelector('#div73');const chargeFill=page.querySelector('#hud-charge-fill');const chargeVal=page.querySelector('#hud-charge-val');const aliveCountEl=page.querySelector('#hud-alive-count');const scoreSummaryEl=page.querySelector('#hud-score-summary');const touchControlsLayer=page.querySelector('#touch-controls-layer');const crosshair=page.querySelector('#crosshair');const specControls=page.querySelector('#spectator-controls');const specTargetName=page.querySelector('#spec-target-name-display');const specTargetHp=page.querySelector('#spec-target-hp-display');const specTargetScore=page.querySelector('#spec-target-score-display');const specPrevBtn=page.querySelector('#spec-prev-btn');const specNextBtn=page.querySelector('#spec-next-btn');const specCheerBtn=page.querySelector('#spec-cheer-btn');const ragdollOverlay=page.querySelector('#ragdoll-feedback-overlay');function cycleSpectateTarget(direction){if(!gamePlayers||gamePlayers.length===0)return;let candidateList=gamePlayers.filter(function(p){return!p.isDead});if(candidateList.length===0)candidateList=gamePlayers.slice();let currentIdxInCandidates=candidateList.findIndex(function(p){return p.playerIndex===spectateTargetIdx});if(currentIdxInCandidates===-1)currentIdxInCandidates=0;let nextIdxInCandidates=currentIdxInCandidates+direction;if(nextIdxInCandidates<0){nextIdxInCandidates=candidateList.length-1}else if(nextIdxInCandidates>=candidateList.length){nextIdxInCandidates=0}
const nextTarget=candidateList[nextIdxInCandidates];if(nextTarget){spectateTargetIdx=nextTarget.playerIndex;updateSpectatorHUD();if(window.FloppySound&&window.FloppySound.playGetUp){window.FloppySound.playGetUp()}}}
function updateSpectatorHUD(){const targetPlayer=gamePlayers.find(function(p){return p.playerIndex===spectateTargetIdx})||gamePlayers[0];if(!targetPlayer)return;if(specTargetName){const isSelf=targetPlayer.isMe?' (あなた)':'';specTargetName.textContent=targetPlayer.data.name+isSelf+(targetPlayer.data.isBot?' 🤖':' 👤')}
if(specTargetHp){const hpNum=Math.max(0,Math.round(targetPlayer.hp));specTargetHp.innerHTML='<i class="fa-solid fa-heart"></i> '+hpNum+'%'+(targetPlayer.isDead?' (脱落)':'')}
if(specTargetScore){specTargetScore.innerHTML='<i class="fa-solid fa-trophy"></i> '+(targetPlayer.score||0)+' pt'}}
function bindSpecNavButton(btnElement,direction){if(!btnElement)return;let isHandled=!1;const triggerNav=function(e){if(e){e.preventDefault();e.stopPropagation()}
if(isHandled)return;isHandled=!0;setTimeout(function(){isHandled=!1},150);cycleSpectateTarget(direction)};btnElement.addEventListener('pointerdown',triggerNav,{passive:!1});btnElement.addEventListener('touchstart',triggerNav,{passive:!1});btnElement.addEventListener('click',triggerNav)}
bindSpecNavButton(specPrevBtn,-1);bindSpecNavButton(specNextBtn,1);const onSpecKeyDown=function(e){if(!isSpectating)return;if(e.code==='KeyQ'||e.code==='ArrowLeft'){e.preventDefault();cycleSpectateTarget(-1)}};window.addEventListener('keydown',onSpecKeyDown);if(specCheerBtn){const cheerAction=function(e){if(e){e.preventDefault();e.stopPropagation()}
const target=gamePlayers.find(function(p){return p.playerIndex===spectateTargetIdx});if(target&&!target.isDead){spawnWinnerConfetti(confettiSystem,target.pos,30);if(window.FloppySound&&window.FloppySound.playGetUp)window.FloppySound.playGetUp();addKillFeed(page,'CHEER!!',target.data.name+' に声援を送った！❄️')}};specCheerBtn.addEventListener('pointerdown',cheerAction);specCheerBtn.addEventListener('touchstart',cheerAction);specCheerBtn.addEventListener('click',cheerAction)}
function enterSpectatorMode(){if(isSpectating)return;isSpectating=!0;if(touchControlsLayer)touchControlsLayer.classList.add('hidden');if(crosshair)crosshair.classList.add('hidden');if(ragdollOverlay)ragdollOverlay.classList.add('hidden');if(specControls)specControls.classList.remove('hidden');const alive=gamePlayers.filter(function(p){return!p.isDead&&!p.isMe});if(alive.length>0){spectateTargetIdx=alive[0].playerIndex}else{const anyAlive=gamePlayers.filter(function(p){return!p.isDead});spectateTargetIdx=anyAlive.length>0?anyAlive[0].playerIndex:0}
updateSpectatorHUD()}
const lobbyBtn=page.querySelector('#result-lobby-btn');if(lobbyBtn){lobbyBtn.addEventListener('click',function(e){e.stopPropagation();window.handleGameLeave()})}
const quitBtn=page.querySelector('#game-quit-btn');if(quitBtn){quitBtn.addEventListener('click',function(e){e.stopPropagation();window.handleGameLeave()})}
function startCountdown(){gameStarted=!1;isGameFinishing=!1;isSpectating=!1;timeScale=1.0;matchWinner=null;matchTimeRemaining=settings.matchTimeSec||120;let countdownVal=3;if(countdownEl)countdownEl.classList.remove('hidden');if(finishOverlay)finishOverlay.classList.add('hidden');if(specControls)specControls.classList.add('hidden');if(touchControlsLayer)touchControlsLayer.classList.remove('hidden');if(crosshair)crosshair.classList.remove('hidden');if(countNumEl)countNumEl.textContent='3';if(window.FloppySound)window.FloppySound.playCountdown(!1);if(countInterval)clearInterval(countInterval);countInterval=setInterval(function(){countdownVal--;if(countdownVal>0){if(countNumEl)countNumEl.textContent=countdownVal;if(window.FloppySound)window.FloppySound.playCountdown(!1);}else if(countdownVal===0){if(countNumEl)countNumEl.textContent='GO!';if(window.FloppySound)window.FloppySound.playCountdown(!0);}else{clearInterval(countInterval);countInterval=null;if(countdownEl)countdownEl.classList.add('hidden');gameStarted=!0}},1000)}
startCountdown();function gameLoop(now){if(!isRunning)return;requestAnimationFrame(gameLoop);try{const rawDt=Math.min((now-lastTime)/1000,0.05);lastTime=now;const dt=rawDt*timeScale;if(gameStarted){if(!isGameFinishing){if(settings.matchTimeSec>0){matchTimeRemaining-=dt;if(matchTimeRemaining<=0){matchTimeRemaining=0;triggerDramaticFinish('timeup');broadcastP2P({t:7,type:'FINISH_MATCH',reason:'timeup'})}
if(timerDisplay){const mins=Math.floor(matchTimeRemaining/60);const secs=Math.floor(matchTimeRemaining%60);timerDisplay.textContent=(mins<10?'0':'')+mins+':'+(secs<10?'0':'')+secs}}else if(timerDisplay){timerDisplay.textContent='∞'}}
p2pSyncAccumulator+=rawDt;if(p2pSyncAccumulator>=0.05&&myPlayerObj){p2pSyncAccumulator=0;broadcastP2P({t:4,idx:myPlayerIdx,p:[Math.round(myPlayerObj.pos.x*100)/100,Math.round(myPlayerObj.pos.y*100)/100,Math.round(myPlayerObj.pos.z*100)/100],v:[Math.round(myPlayerObj.vel.x*10)/10,Math.round(myPlayerObj.vel.y*10)/10,Math.round(myPlayerObj.vel.z*10)/10],r:Math.round(myPlayerObj.rotationY*100)/100,hp:Math.round(myPlayerObj.hp),score:myPlayerObj.score||0,isDown:!!myPlayerObj.isDown,isDiving:!!myPlayerObj.isDiving,isDead:!!myPlayerObj.isDead})}
gamePlayers.forEach(function(p){if(p.isDead)return;if(p.invincibleTimer>0){p.invincibleTimer-=dt}
if(isGameFinishing&&matchWinner===p){p.vel.set(0,0,0);p.pos.y=1.0+Math.abs(Math.sin(now*0.009))*1.6;p.mesh.rotation.y+=dt*4.5;if(p.leftArm)p.leftArm.rotation.z=Math.PI*0.75+Math.sin(now*0.015)*0.4;if(p.rightArm)p.rightArm.rotation.z=-Math.PI*0.75-Math.sin(now*0.015)*0.4}else if(p.isMe&&!isGameFinishing){updatePlayerPhysics(p,inputState,dt,terrainColliders,gamePlayers,particles,page,broadcastP2P)}else if(p.data.isBot&&!isGameFinishing){updateBotAI(p,gamePlayers,dt,terrainColliders,scene,snowballs,healItems,particles,page)}else if(!p.isMe&&!isGameFinishing){if(window.updateRemotePlayerAnimation){window.updateRemotePlayerAnimation(p,dt)}}
if(!isGameFinishing){healItems.forEach(function(item){if(item.active&&p.pos.distanceTo(item.mesh.position)<1.8){item.active=!1;item.mesh.visible=!1;item.respawnTimer=15.0;p.hp=Math.min(100,p.hp+30);spawnHitParticles(particles,item.mesh.position,0x10b981,15);if(window.FloppySound&&window.FloppySound.playGetUp)window.FloppySound.playGetUp();}})}
if(p.hp<=0&&!p.isDead){p.hp=0;p.isDead=!0;p.mesh.rotation.z=Math.PI/2;spawnHitParticles(particles,p.pos,0xf43f5e,35);addKillFeed(page,'K.O.!!',p.data.name+' は力尽きた！');if(p.isMe){broadcastP2P({t:8,type:'PLAYER_DEAD',idx:p.playerIndex,reason:'knockout'});enterSpectatorMode()}}
if(p.pos.y<-12.0&&!p.isDead){p.isDead=!0;p.hp=0;spawnHitParticles(particles,p.pos,0x38bdf8,40);addKillFeed(page,'RING OUT!!',p.data.name+' が転落！');if(p.isMe){broadcastP2P({t:8,type:'PLAYER_DEAD',idx:p.playerIndex,reason:'ringout'});enterSpectatorMode()}}});if(isSpectating){const curTarget=gamePlayers.find(function(p){return p.playerIndex===spectateTargetIdx});if(!curTarget||curTarget.isDead){const nextAlive=gamePlayers.find(function(p){return!p.isDead});if(nextAlive){spectateTargetIdx=nextAlive.playerIndex}}
updateSpectatorHUD()}
updateSnowballs(snowballs,gamePlayers,terrainColliders,dt,particles,page,function(hitData){broadcastP2P({t:6,type:'DAMAGE_EVENT',targetIdx:hitData.targetIdx,damage:hitData.damage,impulse:hitData.impulse,shooterIdx:hitData.shooterIdx,shooterScore:hitData.shooterScore,targetHp:hitData.targetHp})});const alivePlayers=gamePlayers.filter(function(p){return!p.isDead});if(aliveCountEl)aliveCountEl.textContent='生存: '+alivePlayers.length+' / '+gamePlayers.length;if(alivePlayers.length<=1&&gamePlayers.length>1&&!isGameFinishing){const winP=alivePlayers.length===1?alivePlayers[0]:null;const winIdx=winP?winP.playerIndex:0;triggerDramaticFinish('knockout',winIdx);broadcastP2P({t:7,type:'FINISH_MATCH',winnerIdx:winIdx,reason:'knockout'})}}
if(myPlayerObj&&hpFill&&hpVal){const hpPercent=Math.max(0,Math.round(myPlayerObj.hp));hpFill.style.width=hpPercent+'%';hpVal.textContent=hpPercent+'%';if(hpPercent<=30&&hpBox){hpBox.classList.add('hp-danger')}else if(hpBox){hpBox.classList.remove('hp-danger')}
if(scoreSummaryEl)scoreSummaryEl.textContent='スコア: '+(myPlayerObj.score||0)+' pt'}
if(chargeFill&&chargeVal){chargeFill.style.width=Math.round(inputState.chargeRate*100)+'%';chargeVal.textContent=Math.round(inputState.chargeRate*100)+'%'}
updateParticles(particles,dt);updateConfetti(confettiSystem,dt);if(isGameFinishing&&matchWinner){finishCamAngle+=dt*2.2;const center=matchWinner.pos.clone().add(new THREE.Vector3(0,1.2,0));const camDist=5.0;const camX=center.x+Math.sin(finishCamAngle)*camDist;const camZ=center.z+Math.cos(finishCamAngle)*camDist;const camY=center.y+2.0+Math.sin(finishCamAngle*0.5)*0.8;camera.position.lerp(new THREE.Vector3(camX,camY,camZ),0.15);camera.lookAt(center)}else if(isSpectating){const targetPlayer=gamePlayers.find(function(p){return p.playerIndex===spectateTargetIdx})||gamePlayers[0];updateFollowCamera(camera,targetPlayer,inputState)}else if(myPlayerObj){updateFollowCamera(camera,myPlayerObj,inputState)}
renderer.render(scene,camera)}catch(err){console.warn('Render loop frame error:',err)}}
requestAnimationFrame(gameLoop);function onWindowResize(){if(!container)return;const w=Math.max(container.clientWidth||window.innerWidth,200);const h=Math.max(container.clientHeight||window.innerHeight,200);camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h)}
window.addEventListener('resize',onWindowResize);page._cleanupGame=function(){isRunning=!1;gameStarted=!1;if(countInterval)clearInterval(countInterval);window.removeEventListener('resize',onWindowResize);window.removeEventListener('keydown',onSpecKeyDown);if(page._cleanupInputControls){page._cleanupInputControls()}
if(renderer&&renderer.domElement&&renderer.domElement.parentNode){renderer.domElement.parentNode.removeChild(renderer.domElement)}}}
window.initGamePage=initGamePage;function buildStageTerrain(scene,stageId,terrainColliders){const isCliff=(stageId==='cliff');if(!isCliff){const mainRadius=45;const groundGeo=new THREE.CylinderGeometry(mainRadius,mainRadius+3,4,48);const groundMat=new THREE.MeshStandardMaterial({color:0x93c5fd,roughness:0.12,metalness:0.35});const groundMesh=new THREE.Mesh(groundGeo,groundMat);groundMesh.position.y=-2;groundMesh.receiveShadow=!0;scene.add(groundMesh);terrainColliders.push({type:'platform',radius:mainRadius,centerX:0,centerZ:0,topY:0});const towerBase=new THREE.Mesh(new THREE.CylinderGeometry(3.5,4.5,3,16),new THREE.MeshStandardMaterial({color:0xe0f2fe,roughness:0.8}));towerBase.position.set(0,1.5,0);towerBase.castShadow=!0;towerBase.receiveShadow=!0;scene.add(towerBase);const towerTop=new THREE.Mesh(new THREE.SphereGeometry(2.5,16,16),new THREE.MeshStandardMaterial({color:0xffffff,roughness:0.9}));towerTop.position.set(0,4.5,0);towerTop.castShadow=!0;scene.add(towerTop);terrainColliders.push({type:'obstacle',name:'tower',x:0,z:0,radius:4.2,minY:0,maxY:6.5});const obstacleCount=18;for(let i=0;i<obstacleCount;i++){const ang=(i/obstacleCount)*Math.PI*2+(i%2)*0.2;const dist=14+(i%3)*9;const ox=Math.cos(ang)*dist;const oz=Math.sin(ang)*dist;if(i%3===0){const domeGeo=new THREE.SphereGeometry(3.0,16,16,0,Math.PI*2,0,Math.PI*0.5);const domeMat=new THREE.MeshStandardMaterial({color:0xf8fafc,roughness:0.9});const dome=new THREE.Mesh(domeGeo,domeMat);dome.position.set(ox,0,oz);dome.castShadow=!0;scene.add(dome);terrainColliders.push({type:'obstacle',name:'igloo',x:ox,z:oz,radius:3.0,minY:0,maxY:3.0})}else if(i%3===1){const h=4.0+Math.random()*2.0;const r=1.4+Math.random()*0.6;const iceGeo=new THREE.ConeGeometry(r,h,6);const iceMat=new THREE.MeshStandardMaterial({color:0x38bdf8,roughness:0.2,transparent:!0,opacity:0.85});const ice=new THREE.Mesh(iceGeo,iceMat);ice.position.set(ox,h/2,oz);ice.castShadow=!0;scene.add(ice);terrainColliders.push({type:'obstacle',name:'ice_spike',x:ox,z:oz,radius:r*1.1,minY:0,maxY:h})}else{const treeGroup=new THREE.Group();const trunk=new THREE.Mesh(new THREE.CylinderGeometry(0.3,0.4,1.5,8),new THREE.MeshStandardMaterial({color:0x78350f}));trunk.position.y=0.75;treeGroup.add(trunk);const leaves=new THREE.Mesh(new THREE.ConeGeometry(1.8,3.5,8),new THREE.MeshStandardMaterial({color:0x065f46}));leaves.position.y=2.8;leaves.castShadow=!0;treeGroup.add(leaves);treeGroup.position.set(ox,0,oz);scene.add(treeGroup);terrainColliders.push({type:'obstacle',name:'tree',x:ox,z:oz,radius:1.8,minY:0,maxY:4.5})}}}else{const mainRadius=26;const groundGeo=new THREE.CylinderGeometry(mainRadius,mainRadius-4,6,32);const groundMat=new THREE.MeshStandardMaterial({color:0xe2e8f0,roughness:0.85});const mainMesh=new THREE.Mesh(groundGeo,groundMat);mainMesh.position.y=-3;mainMesh.receiveShadow=!0;scene.add(mainMesh);terrainColliders.push({type:'platform',radius:mainRadius,centerX:0,centerZ:0,topY:0});const islandOffsets=[{x:34,z:0,r:10},{x:-34,z:0,r:10},{x:0,z:34,r:10},{x:0,z:-34,r:10}];islandOffsets.forEach(function(isl){const islGeo=new THREE.CylinderGeometry(isl.r,isl.r-2,5,24);const islMat=new THREE.MeshStandardMaterial({color:0xcfd8dc,roughness:0.9});const islMesh=new THREE.Mesh(islGeo,islMat);islMesh.position.set(isl.x,-2.5,isl.z);islMesh.receiveShadow=!0;scene.add(islMesh);terrainColliders.push({type:'platform',radius:isl.r,centerX:isl.x,centerZ:isl.z,topY:0});const bridgeGeo=new THREE.BoxGeometry(Math.abs(isl.x||isl.z)-mainRadius,0.4,3.5);const bridgeMat=new THREE.MeshStandardMaterial({color:0x7dd3fc,roughness:0.2,transparent:!0,opacity:0.9});const bridgeMesh=new THREE.Mesh(bridgeGeo,bridgeMat);bridgeMesh.position.set(isl.x/2,-0.2,isl.z/2);if(isl.z!==0)bridgeMesh.rotation.y=Math.PI/2;bridgeMesh.receiveShadow=!0;scene.add(bridgeMesh);terrainColliders.push({type:'bridge',minX:Math.min(isl.x,0)-2,maxX:Math.max(isl.x,0)+2,minZ:Math.min(isl.z,0)-2,maxZ:Math.max(isl.z,0)+2,topY:0});terrainColliders.push({type:'obstacle',name:'cliff_rock',x:isl.x,z:isl.z,radius:2.2,minY:0,maxY:3.5})})}}
window.buildStageTerrain=buildStageTerrain;function createFloppyCharacter(scene,data,x,y,z){const group=new THREE.Group();const mainColor=new THREE.Color(data.color||'#38bdf8');const skinMat=new THREE.MeshStandardMaterial({color:0xffedd5,roughness:0.6});const suitMat=new THREE.MeshStandardMaterial({color:mainColor,roughness:0.5,metalness:0.1});const darkMat=new THREE.MeshStandardMaterial({color:0x1e293b,roughness:0.7});const whiteMat=new THREE.MeshStandardMaterial({color:0xf8fafc,roughness:0.8});const squashRoot=new THREE.Group();group.add(squashRoot);const pelvisGroup=new THREE.Group();pelvisGroup.position.y=0.5;squashRoot.add(pelvisGroup);const spineGroup=new THREE.Group();pelvisGroup.add(spineGroup);const torsoGeo=new THREE.CylinderGeometry(0.34,0.42,0.55,12);const torsoMesh=new THREE.Mesh(torsoGeo,suitMat);torsoMesh.position.y=0.28;torsoMesh.castShadow=!0;spineGroup.add(torsoMesh);const bellyGeo=new THREE.SphereGeometry(0.3,12,12);bellyGeo.scale(0.9,0.95,0.6);const bellyMesh=new THREE.Mesh(bellyGeo,whiteMat);bellyMesh.position.set(0,0.24,0.22);spineGroup.add(bellyMesh);const scarfGeo=new THREE.CylinderGeometry(0.4,0.38,0.14,12);const scarfMat=new THREE.MeshStandardMaterial({color:0xf43f5e,roughness:0.8});const scarfMesh=new THREE.Mesh(scarfGeo,scarfMat);scarfMesh.position.set(0,0.58,0);spineGroup.add(scarfMesh);const scarfTailPivot=new THREE.Group();scarfTailPivot.position.set(0.18,0.56,-0.22);const scarfTailGeo=new THREE.BoxGeometry(0.14,0.38,0.05);const scarfTailMesh=new THREE.Mesh(scarfTailGeo,scarfMat);scarfTailMesh.position.set(0,-0.16,0);scarfTailPivot.add(scarfTailMesh);spineGroup.add(scarfTailPivot);const headPivot=new THREE.Group();headPivot.position.set(0,0.68,0);spineGroup.add(headPivot);const headGeo=new THREE.SphereGeometry(0.35,14,14);const headMesh=new THREE.Mesh(headGeo,skinMat);headMesh.position.y=0.2;headMesh.castShadow=!0;headPivot.add(headMesh);const hatGeo=new THREE.SphereGeometry(0.36,12,12,0,Math.PI*2,0,Math.PI*0.58);const hatMat=new THREE.MeshStandardMaterial({color:0xf59e0b,roughness:0.7});const hatMesh=new THREE.Mesh(hatGeo,hatMat);hatMesh.position.set(0,0.26,0);headPivot.add(hatMesh);const pomPivot=new THREE.Group();pomPivot.position.set(0,0.62,0);const pomGeo=new THREE.SphereGeometry(0.13,8,8);const pomMesh=new THREE.Mesh(pomGeo,whiteMat);pomPivot.add(pomMesh);headPivot.add(pomPivot);const eyeGeo=new THREE.SphereGeometry(0.055,8,8);const leftEye=new THREE.Mesh(eyeGeo,darkMat);leftEye.position.set(-0.11,0.22,0.32);const rightEye=new THREE.Mesh(eyeGeo,darkMat);rightEye.position.set(0.11,0.22,0.32);headPivot.add(leftEye);headPivot.add(rightEye);const cheekMat=new THREE.MeshBasicMaterial({color:0xfb7185});const cheekGeo=new THREE.SphereGeometry(0.045,6,6);const leftCheek=new THREE.Mesh(cheekGeo,cheekMat);leftCheek.position.set(-0.18,0.14,0.28);const rightCheek=new THREE.Mesh(cheekGeo,cheekMat);rightCheek.position.set(0.18,0.14,0.28);headPivot.add(leftCheek);headPivot.add(rightCheek);function createFloppyArm(isLeft){const shoulder=new THREE.Group();shoulder.position.set(isLeft?-0.44:0.44,0.48,0);const upperArmGeo=new THREE.CylinderGeometry(0.09,0.08,0.22,8);const upperArm=new THREE.Mesh(upperArmGeo,suitMat);upperArm.position.y=-0.1;upperArm.castShadow=!0;shoulder.add(upperArm);const elbow=new THREE.Group();elbow.position.set(0,-0.2,0);shoulder.add(elbow);const lowerArmGeo=new THREE.CylinderGeometry(0.08,0.07,0.2,8);const lowerArm=new THREE.Mesh(lowerArmGeo,suitMat);lowerArm.position.y=-0.09;lowerArm.castShadow=!0;elbow.add(lowerArm);const mittenGeo=new THREE.SphereGeometry(0.12,8,8);const mitten=new THREE.Mesh(mittenGeo,darkMat);mitten.position.set(0,-0.22,0.03);mitten.castShadow=!0;elbow.add(mitten);return{shoulder:shoulder,elbow:elbow}}
const leftArmObj=createFloppyArm(!0);const rightArmObj=createFloppyArm(!1);spineGroup.add(leftArmObj.shoulder);spineGroup.add(rightArmObj.shoulder);function createFloppyLeg(isLeft){const hip=new THREE.Group();hip.position.set(isLeft?-0.17:0.17,0,0);const thighGeo=new THREE.CylinderGeometry(0.11,0.09,0.24,8);const thigh=new THREE.Mesh(thighGeo,suitMat);thigh.position.y=-0.11;thigh.castShadow=!0;hip.add(thigh);const knee=new THREE.Group();knee.position.set(0,-0.22,0);hip.add(knee);const shinGeo=new THREE.CylinderGeometry(0.09,0.08,0.22,8);const shin=new THREE.Mesh(shinGeo,suitMat);shin.position.y=-0.1;shin.castShadow=!0;knee.add(shin);const bootGeo=new THREE.BoxGeometry(0.18,0.15,0.28);const boot=new THREE.Mesh(bootGeo,darkMat);boot.position.set(0,-0.22,0.05);boot.castShadow=!0;knee.add(boot);return{hip:hip,knee:knee}}
const leftLegObj=createFloppyLeg(!0);const rightLegObj=createFloppyLeg(!1);pelvisGroup.add(leftLegObj.hip);pelvisGroup.add(rightLegObj.hip);let nameSprite=null;try{const canvas=document.createElement('canvas');canvas.width=256;canvas.height=64;const ctx=canvas.getContext('2d');if(ctx){ctx.fillStyle='rgba(15, 23, 42, 0.85)';const bx=10,by=10,bw=236,bh=44,br=12;ctx.beginPath();ctx.moveTo(bx+br,by);ctx.lineTo(bx+bw-br,by);ctx.quadraticCurveTo(bx+bw,by,bx+bw,by+br);ctx.lineTo(bx+bw,by+bh-br);ctx.quadraticCurveTo(bx+bw,by+bh,bx+bw-br,by+bh);ctx.lineTo(bx+br,by+bh);ctx.quadraticCurveTo(bx,by+bh,bx,by+bh-br);ctx.lineTo(bx,by+br);ctx.quadraticCurveTo(bx,by,bx+br,by);ctx.closePath();ctx.fill();ctx.font='bold 24px sans-serif';ctx.fillStyle='#ffffff';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(data.name||'プレイヤー',128,32);const nameTex=new THREE.CanvasTexture(canvas);const nameMat=new THREE.SpriteMaterial({map:nameTex,transparent:!0});nameSprite=new THREE.Sprite(nameMat);nameSprite.scale.set(2,0.5,1);nameSprite.position.y=2.2;group.add(nameSprite)}}catch(e){}
group.position.set(x,y,z);scene.add(group);return{data:data,mesh:group,nameSprite:nameSprite,squashRoot:squashRoot,pelvisGroup:pelvisGroup,spineGroup:spineGroup,bellyMesh:bellyMesh,headPivot:headPivot,pomPivot:pomPivot,scarfTailPivot:scarfTailPivot,leftArm:leftArmObj.shoulder,leftElbow:leftArmObj.elbow,rightArm:rightArmObj.shoulder,rightElbow:rightArmObj.elbow,leftLeg:leftLegObj.hip,leftKnee:leftLegObj.knee,rightLeg:rightLegObj.hip,rightKnee:rightLegObj.knee,pos:group.position,vel:new THREE.Vector3(0,0,0),rotationY:0,tilt:0,hp:100,score:0,isDown:!1,downTimer:0,isDead:!1,invincibleTimer:0,wobblePhase:Math.random()*Math.PI,spineSpring:{pitch:0,roll:0,yaw:0,vPitch:0,vRoll:0,vYaw:0},headSpring:{pitch:0,roll:0,yaw:0,vPitch:0,vRoll:0,vYaw:0},pomSpring:{x:0,z:0,vx:0,vz:0},scarfSpring:{pitch:0,roll:0,vPitch:0,vRoll:0},squashScale:{y:1.0,xz:1.0,vy:0},jellyOscillation:0}}
window.createFloppyCharacter=createFloppyCharacter;function setupGameInputControls(page,inputState,onDive,onThrow){const container=page.querySelector('#three-canvas-container');const isTouchDevice=('ontouchstart' in window)||(navigator.maxTouchPoints>0)||(window.matchMedia&&window.matchMedia('(hover: none) and (pointer: coarse)').matches);const onKeyDown=function(e){inputState.keys[e.code]=!0;if(e.code==='Space'){e.preventDefault();onDive()}};const onKeyUp=function(e){inputState.keys[e.code]=!1};window.addEventListener('keydown',onKeyDown);window.addEventListener('keyup',onKeyUp);const isUiElement=function(el){if(!el)return!1;return!!el.closest('#virtual-joystick-zone, .game-touch-btn, .camera-reset-btn, .hud-quit-btn, .spectator-controls-layer, .modal-backdrop, .hud-top-bar')};let isPointerLocked=!1;let isMouseDragging=!1;let lastMouseX=0;let lastMouseY=0;let isMouseCharging=!1;let mouseChargeStart=0;const onPointerLockChange=function(){isPointerLocked=(document.pointerLockElement===container||document.mozPointerLockElement===container||document.webkitPointerLockElement===container)};if(!isTouchDevice){document.addEventListener('pointerlockchange',onPointerLockChange);document.addEventListener('mozpointerlockchange',onPointerLockChange);document.addEventListener('webkitpointerlockchange',onPointerLockChange)}
const onMouseDown=function(e){if(isTouchDevice||isUiElement(e.target))return;if(container&&!isPointerLocked&&(e.button===0||e.button===2)){try{if(container.requestPointerLock){container.requestPointerLock()}else if(container.mozRequestPointerLock){container.mozRequestPointerLock()}else if(container.webkitRequestPointerLock){container.webkitRequestPointerLock()}}catch(err){}}
isMouseDragging=!0;lastMouseX=e.clientX;lastMouseY=e.clientY;if(e.button===0){isMouseCharging=!0;mouseChargeStart=performance.now();inputState.isCharging=!0;inputState.chargeStart=mouseChargeStart}};const onMouseMove=function(e){if(isTouchDevice)return;let dx=0;let dy=0;if(isPointerLocked){dx=e.movementX||e.mozMovementX||e.webkitMovementX||0;dy=e.movementY||e.mozMovementY||e.webkitMovementY||0;inputState.cameraYaw-=dx*0.0035;inputState.cameraPitch=Math.max(0.05,Math.min(1.25,inputState.cameraPitch+dy*0.0035))}else if(isMouseDragging){dx=e.clientX-lastMouseX;dy=e.clientY-lastMouseY;lastMouseX=e.clientX;lastMouseY=e.clientY;inputState.cameraYaw-=dx*0.006;inputState.cameraPitch=Math.max(0.05,Math.min(1.25,inputState.cameraPitch+dy*0.006))}};const onMouseUp=function(e){if(isTouchDevice)return;isMouseDragging=!1;if(isMouseCharging&&e.button===0){const duration=(performance.now()-mouseChargeStart)/1000;isMouseCharging=!1;inputState.isCharging=!1;inputState.chargeRate=0;onThrow(Math.max(0.05,duration))}};const onContextMenu=function(e){if(!isUiElement(e.target)){e.preventDefault()}};if(!isTouchDevice){window.addEventListener('mousedown',onMouseDown);window.addEventListener('mousemove',onMouseMove);window.addEventListener('mouseup',onMouseUp)}
window.addEventListener('contextmenu',onContextMenu);const stickZone=page.querySelector('#virtual-joystick-zone');const stickThumb=page.querySelector('#joystick-thumb');let stickTouchId=null;let stickOriginX=0;let stickOriginY=0;function updateStick(cx,cy){const dx=cx-stickOriginX;const dy=cy-stickOriginY;const dist=Math.hypot(dx,dy);const maxR=45;const angle=Math.atan2(dy,dx);const clampedDist=Math.min(dist,maxR);const px=Math.cos(angle)*clampedDist;const py=Math.sin(angle)*clampedDist;if(stickThumb)stickThumb.style.transform='translate('+px+'px, '+py+'px)';if(clampedDist>maxR*0.1){inputState.moveX=px/maxR;inputState.moveZ=py/maxR}else{inputState.moveX=0;inputState.moveZ=0}}
const onStickTouchStart=function(e){e.preventDefault();e.stopPropagation();const touch=e.changedTouches[0];stickTouchId=touch.identifier;const rect=stickZone.getBoundingClientRect();stickOriginX=rect.left+rect.width/2;stickOriginY=rect.top+rect.height/2;updateStick(touch.clientX,touch.clientY)};const onStickTouchMove=function(e){for(let i=0;i<e.changedTouches.length;i++){const touch=e.changedTouches[i];if(touch.identifier===stickTouchId){e.preventDefault();updateStick(touch.clientX,touch.clientY)}}};const onStickTouchEnd=function(e){for(let i=0;i<e.changedTouches.length;i++){if(e.changedTouches[i].identifier===stickTouchId){stickTouchId=null;inputState.moveX=0;inputState.moveZ=0;if(stickThumb)stickThumb.style.transform='translate(0px, 0px)'}}};if(stickZone){stickZone.addEventListener('touchstart',onStickTouchStart,{passive:!1});window.addEventListener('touchmove',onStickTouchMove,{passive:!1});window.addEventListener('touchend',onStickTouchEnd,{passive:!0});window.addEventListener('touchcancel',onStickTouchEnd,{passive:!0})}
let cameraTouchId=null;let lastCamTouchX=0;let lastCamTouchY=0;const onCameraTouchStart=function(e){for(let i=0;i<e.changedTouches.length;i++){const touch=e.changedTouches[i];if(cameraTouchId===null&&!isUiElement(touch.target)){cameraTouchId=touch.identifier;lastCamTouchX=touch.clientX;lastCamTouchY=touch.clientY}}};const onCameraTouchMove=function(e){if(cameraTouchId===null)return;for(let i=0;i<e.changedTouches.length;i++){const touch=e.changedTouches[i];if(touch.identifier===cameraTouchId){const dx=touch.clientX-lastCamTouchX;const dy=touch.clientY-lastCamTouchY;lastCamTouchX=touch.clientX;lastCamTouchY=touch.clientY;inputState.cameraYaw-=dx*0.0085;inputState.cameraPitch=Math.max(0.05,Math.min(1.25,inputState.cameraPitch+dy*0.0085))}}};const onCameraTouchEnd=function(e){for(let i=0;i<e.changedTouches.length;i++){if(e.changedTouches[i].identifier===cameraTouchId){cameraTouchId=null}}};window.addEventListener('touchstart',onCameraTouchStart,{passive:!0});window.addEventListener('touchmove',onCameraTouchMove,{passive:!0});window.addEventListener('touchend',onCameraTouchEnd,{passive:!0});window.addEventListener('touchcancel',onCameraTouchEnd,{passive:!0});const resetCamBtn=page.querySelector('#camera-reset-btn');const onResetCam=function(e){if(e){e.preventDefault();e.stopPropagation()}
const topPage=document.querySelector('#appNavigator').topPage;const myPlayer=(topPage&&topPage._getMyPlayer)?topPage._getMyPlayer():null;if(myPlayer){inputState.cameraYaw=myPlayer.rotationY?(myPlayer.rotationY-Math.PI):0;inputState.cameraPitch=0.28}else{inputState.cameraPitch=0.28}};if(resetCamBtn){resetCamBtn.addEventListener('pointerdown',onResetCam);resetCamBtn.addEventListener('touchstart',onResetCam,{passive:!1});resetCamBtn.addEventListener('click',onResetCam)}
const diveBtn=page.querySelector('#dive-action-btn');let isDiveHandled=!1;const handleDiveAction=function(e){if(e){e.preventDefault();e.stopPropagation()}
if(isDiveHandled)return;isDiveHandled=!0;setTimeout(function(){isDiveHandled=!1},200);onDive()};if(diveBtn){diveBtn.addEventListener('pointerdown',handleDiveAction);diveBtn.addEventListener('touchstart',handleDiveAction,{passive:!1});diveBtn.addEventListener('click',handleDiveAction)}
const throwBtn=page.querySelector('#throw-action-btn');let buttonChargeStart=0;let isBtnCharging=!1;const handleThrowStart=function(e){if(e){e.preventDefault();e.stopPropagation()}
isBtnCharging=!0;buttonChargeStart=performance.now();inputState.isCharging=!0;inputState.chargeStart=buttonChargeStart};const handleThrowEnd=function(e){if(e){e.preventDefault();e.stopPropagation()}
if(isBtnCharging){const duration=(performance.now()-buttonChargeStart)/1000;isBtnCharging=!1;inputState.isCharging=!1;inputState.chargeRate=0;onThrow(Math.max(0.05,duration))}};if(throwBtn){throwBtn.addEventListener('pointerdown',handleThrowStart);throwBtn.addEventListener('pointerup',handleThrowEnd);throwBtn.addEventListener('touchstart',handleThrowStart,{passive:!1});throwBtn.addEventListener('touchend',handleThrowEnd,{passive:!1});if(!isTouchDevice){throwBtn.addEventListener('mousedown',handleThrowStart);throwBtn.addEventListener('mouseup',handleThrowEnd)}}
page._cleanupInputControls=function(){if(!isTouchDevice&&document.exitPointerLock){try{document.exitPointerLock()}catch(err){}}
if(!isTouchDevice){document.removeEventListener('pointerlockchange',onPointerLockChange);document.removeEventListener('mozpointerlockchange',onPointerLockChange);document.removeEventListener('webkitpointerlockchange',onPointerLockChange);window.removeEventListener('mousedown',onMouseDown);window.removeEventListener('mousemove',onMouseMove);window.removeEventListener('mouseup',onMouseUp)}
window.removeEventListener('keydown',onKeyDown);window.removeEventListener('keyup',onKeyUp);window.removeEventListener('contextmenu',onContextMenu);window.removeEventListener('touchstart',onCameraTouchStart);window.removeEventListener('touchmove',onCameraTouchMove);window.removeEventListener('touchend',onCameraTouchEnd);window.removeEventListener('touchcancel',onCameraTouchEnd);window.removeEventListener('touchmove',onStickTouchMove);window.removeEventListener('touchend',onStickTouchEnd);window.removeEventListener('touchcancel',onStickTouchEnd);if(stickZone)stickZone.removeEventListener('touchstart',onStickTouchStart);if(resetCamBtn){resetCamBtn.removeEventListener('pointerdown',onResetCam);resetCamBtn.removeEventListener('touchstart',onResetCam);resetCamBtn.removeEventListener('click',onResetCam)}
if(diveBtn){diveBtn.removeEventListener('pointerdown',handleDiveAction);diveBtn.removeEventListener('touchstart',handleDiveAction);diveBtn.removeEventListener('click',handleDiveAction)}
if(throwBtn){throwBtn.removeEventListener('pointerdown',handleThrowStart);throwBtn.removeEventListener('pointerup',handleThrowEnd);throwBtn.removeEventListener('touchstart',handleThrowStart);throwBtn.removeEventListener('touchend',handleThrowEnd);if(!isTouchDevice){throwBtn.removeEventListener('mousedown',handleThrowStart);throwBtn.removeEventListener('mouseup',handleThrowEnd)}}}}
window.setupGameInputControls=setupGameInputControls;function updatePlayerPhysics(p,input,dt,terrainColliders,allPlayers,particles,page,broadcastP2P){let onGround=!1;let groundHeight=0;terrainColliders.forEach(function(col){if(col.type==='platform'){const dist=Math.hypot(p.pos.x-col.centerX,p.pos.z-col.centerZ);if(dist<=col.radius){onGround=!0;groundHeight=col.topY}}else if(col.type==='bridge'){if(p.pos.x>=col.minX&&p.pos.x<=col.maxX&&p.pos.z>=col.minZ&&p.pos.z<=col.maxZ){onGround=!0;groundHeight=col.topY}}});if(p.diveCooldown>0){p.diveCooldown-=dt;if(p.diveCooldown<0)p.diveCooldown=0}
if(p.isMe){const diveBtn=document.querySelector('#dive-action-btn');if(diveBtn){if(p.diveCooldown>0){diveBtn.classList.add('on-cooldown');const cdSec=Math.ceil(p.diveCooldown);diveBtn.setAttribute('data-cd',cdSec)}else{diveBtn.classList.remove('on-cooldown')}}}
const kSquash=120.0;const dSquash=10.0;const fSquash=-kSquash*(p.squashScale.y-1.0)-dSquash*p.squashScale.vy;p.squashScale.vy+=fSquash*dt;p.squashScale.y+=p.squashScale.vy*dt;p.squashScale.xz=1.0/Math.sqrt(Math.max(0.2,p.squashScale.y));if(p.squashRoot){p.squashRoot.scale.set(p.squashScale.xz,p.squashScale.y,p.squashScale.xz)}
if(p.isDiving){p.diveTimer-=dt;p.vel.y-=30.0*dt;p.pos.y+=p.vel.y*dt;p.pos.x+=p.vel.x*dt;p.pos.z+=p.vel.z*dt;if(onGround&&p.pos.y<=groundHeight){p.pos.y=groundHeight;p.vel.y=0.0;p.vel.x*=0.92;p.vel.z*=0.92}
p.mesh.rotation.x=THREE.MathUtils.lerp(p.mesh.rotation.x,Math.PI/2.2,0.25);p.mesh.rotation.z=THREE.MathUtils.lerp(p.mesh.rotation.z,0,0.25);if(p.leftArm){p.leftArm.rotation.x=-Math.PI*0.8;p.leftArm.rotation.z=0.2}
if(p.rightArm){p.rightArm.rotation.x=-Math.PI*0.8;p.rightArm.rotation.z=-0.2}
if(particles&&Math.random()<0.4){spawnHitParticles(particles,p.pos,0xffffff,2)}
if(allPlayers){allPlayers.forEach(function(other){if(other===p||other.isDead)return;const d=p.pos.distanceTo(other.pos);if(d<1.2&&d>0.01){const pushVec=p.pos.clone().sub(other.pos).normalize().multiplyScalar(0.1);p.pos.add(pushVec)}})}
if(p.diveTimer<=0){p.isDiving=!1;p.mesh.rotation.x=0;p.vel.y=2.5;p.squashScale.y=1.15;p.squashScale.vy=-4.0;if(p.isMe){const overlay=document.querySelector('#ragdoll-feedback-overlay');if(overlay)overlay.classList.add('hidden');}}
return}
if(p.isDown){p.downTimer-=dt;p.vel.y-=32.0*dt;p.pos.y+=p.vel.y*dt;p.pos.x+=p.vel.x*dt;p.pos.z+=p.vel.z*dt;if(onGround&&p.pos.y<=groundHeight){if(p.pos.y<groundHeight&&Math.abs(p.vel.y)>3.0){p.squashScale.y=0.65;p.squashScale.vy=8.0}
p.pos.y=groundHeight;p.vel.y=-p.vel.y*0.3;p.vel.x*=0.85;p.vel.z*=0.85}else{p.vel.x*=0.98;p.vel.z*=0.98}
p.mesh.rotation.z=THREE.MathUtils.lerp(p.mesh.rotation.z,Math.PI/2.1,0.2);p.mesh.rotation.x=THREE.MathUtils.lerp(p.mesh.rotation.x,Math.PI/6,0.15);if(p.headPivot){p.headPivot.rotation.x=Math.sin(Date.now()*0.008)*0.4+0.3;p.headPivot.rotation.z=Math.cos(Date.now()*0.006)*0.5}
if(p.pomPivot){p.pomPivot.rotation.x=Math.sin(Date.now()*0.015)*0.8;p.pomPivot.rotation.z=Math.cos(Date.now()*0.015)*0.8}
if(p.leftArm)p.leftArm.rotation.z=Math.PI*0.5+Math.sin(Date.now()*0.01)*0.3;if(p.leftElbow)p.leftElbow.rotation.z=Math.PI*0.4+Math.cos(Date.now()*0.012)*0.4;if(p.rightArm)p.rightArm.rotation.z=-Math.PI*0.5-Math.sin(Date.now()*0.01)*0.3;if(p.rightElbow)p.rightElbow.rotation.z=-Math.PI*0.4-Math.cos(Date.now()*0.012)*0.4;if(p.leftLeg)p.leftLeg.rotation.x=Math.PI*0.35+Math.sin(Date.now()*0.008)*0.2;if(p.leftKnee)p.leftKnee.rotation.x=-Math.PI*0.5;if(p.rightLeg)p.rightLeg.rotation.x=-Math.PI*0.2;if(p.rightKnee)p.rightKnee.rotation.x=-Math.PI*0.3;if(p.downTimer<=0&&onGround&&p.pos.y>=groundHeight-0.5){p.isDown=!1;p.mesh.rotation.z=0;p.mesh.rotation.x=0;p.invincibleTimer=0.35;p.vel.y=4.2;p.squashScale.y=1.35;p.squashScale.vy=-6.0;if(p.isMe){const overlay=document.querySelector('#ragdoll-feedback-overlay');if(overlay)overlay.classList.add('hidden');if(window.FloppySound&&window.FloppySound.playGetUp){window.FloppySound.playGetUp()}}}
return}
let kx=0;let kz=0;if(input.keys&&(input.keys.KeyW||input.keys.ArrowUp))kz-=1;if(input.keys&&(input.keys.KeyS||input.keys.ArrowDown))kz+=1;if(input.keys&&(input.keys.KeyA||input.keys.ArrowLeft))kx-=1;if(input.keys&&(input.keys.KeyD||input.keys.ArrowRight))kx+=1;let mx=(input.moveX!==undefined&&input.moveX!==0)?input.moveX:kx;let mz=(input.moveZ!==undefined&&input.moveZ!==0)?input.moveZ:kz;const len=Math.hypot(mx,mz);if(len>1){mx/=len;mz/=len}
if(input.isCharging){const chargeSec=(performance.now()-input.chargeStart)/1000;input.chargeRate=Math.min(1.0,chargeSec/1.5);mx*=0.45;mz*=0.45}
const yaw=input.cameraYaw||0;const worldVx=mx*Math.cos(yaw)+mz*Math.sin(yaw);const worldVz=-mx*Math.sin(yaw)+mz*Math.cos(yaw);const moveSpeed=11.0;const prevVx=p.vel.x;const prevVz=p.vel.z;p.vel.x=THREE.MathUtils.lerp(p.vel.x,worldVx*moveSpeed,0.16);p.vel.z=THREE.MathUtils.lerp(p.vel.z,worldVz*moveSpeed,0.16);const ax=(p.vel.x-prevVx)/Math.max(0.001,dt);const az=(p.vel.z-prevVz)/Math.max(0.001,dt);p.vel.y-=32.0*dt;const prevY=p.pos.y;p.pos.y+=p.vel.y*dt;if(onGround&&p.pos.y<=groundHeight){if(prevY>groundHeight+0.1){p.squashScale.y=0.72;p.squashScale.vy=7.0}
p.pos.y=groundHeight;p.vel.y=0.0}
p.pos.x+=p.vel.x*dt;p.pos.z+=p.vel.z*dt;const speed=Math.hypot(p.vel.x,p.vel.z);const cosR=Math.cos(p.rotationY);const sinR=Math.sin(p.rotationY);const localAx=ax*cosR-az*sinR;const localAz=ax*sinR+az*cosR;const kSpine=80.0;const dSpine=8.5;const targetSpinePitch=-localAz*0.025+(speed>0.5?0.18:0);const targetSpineRoll=localAx*0.03;const fSpinePitch=-kSpine*(p.spineSpring.pitch-targetSpinePitch)-dSpine*p.spineSpring.vPitch;const fSpineRoll=-kSpine*(p.spineSpring.roll-targetSpineRoll)-dSpine*p.spineSpring.vRoll;p.spineSpring.vPitch+=fSpinePitch*dt;p.spineSpring.vRoll+=fSpineRoll*dt;p.spineSpring.pitch+=p.spineSpring.vPitch*dt;p.spineSpring.roll+=p.spineSpring.vRoll*dt;const kHead=65.0;const dHead=7.0;const targetHeadPitch=p.spineSpring.pitch*1.3-localAz*0.015;const targetHeadRoll=-p.spineSpring.roll*1.2;const fHeadPitch=-kHead*(p.headSpring.pitch-targetHeadPitch)-dHead*p.headSpring.vPitch;const fHeadRoll=-kHead*(p.headSpring.roll-targetHeadRoll)-dHead*p.headSpring.vRoll;p.headSpring.vPitch+=fHeadPitch*dt;p.headSpring.vRoll+=fHeadRoll*dt;p.headSpring.pitch+=p.headSpring.vPitch*dt;p.headSpring.roll+=p.headSpring.vRoll*dt;const kPom=140.0;const dPom=6.0;p.pomSpring.vx+=(-kPom*p.pomSpring.x-dPom*p.pomSpring.vx-localAx*0.1)*dt;p.pomSpring.vz+=(-kPom*p.pomSpring.z-dPom*p.pomSpring.vz-localAz*0.1)*dt;p.pomSpring.x+=p.pomSpring.vx*dt;p.pomSpring.z+=p.pomSpring.vz*dt;if(p.pomPivot){p.pomPivot.position.x=THREE.MathUtils.clamp(p.pomSpring.x,-0.3,0.3);p.pomPivot.position.z=THREE.MathUtils.clamp(p.pomSpring.z,-0.3,0.3);p.pomPivot.rotation.z=-p.pomSpring.x*2.0;p.pomPivot.rotation.x=p.pomSpring.z*2.0}
if(p.scarfTailPivot){p.scarfTailPivot.rotation.x=THREE.MathUtils.lerp(p.scarfTailPivot.rotation.x,Math.PI*0.15+speed*0.08+Math.sin(p.wobblePhase*2)*0.2,0.2);p.scarfTailPivot.rotation.z=Math.cos(p.wobblePhase)*0.2}
if(p.bellyMesh){const bellyWobble=Math.sin(p.wobblePhase*1.5)*0.12;p.bellyMesh.scale.set(0.9+bellyWobble,0.95-bellyWobble,0.6+bellyWobble*1.2)}
if(speed>0.3){p.rotationY=THREE.MathUtils.lerp(p.rotationY,Math.atan2(p.vel.x,p.vel.z),0.25);p.mesh.rotation.y=p.rotationY;p.wobblePhase+=dt*(12.0+speed*0.6);const legSwing=Math.sin(p.wobblePhase)*0.85;const armSwing=Math.cos(p.wobblePhase)*0.95;const bounceY=Math.abs(Math.sin(p.wobblePhase))*0.12;if(p.pelvisGroup){p.pelvisGroup.position.y=0.5+bounceY;p.pelvisGroup.rotation.z=Math.sin(p.wobblePhase)*0.12;p.pelvisGroup.rotation.y=Math.cos(p.wobblePhase)*0.15}
if(p.spineGroup){p.spineGroup.rotation.x=p.spineSpring.pitch;p.spineGroup.rotation.z=p.spineSpring.roll+Math.sin(p.wobblePhase)*0.08}
if(p.headPivot){p.headPivot.rotation.x=p.headSpring.pitch;p.headPivot.rotation.z=p.headSpring.roll+Math.cos(p.wobblePhase)*0.1}
if(p.leftArm){p.leftArm.rotation.x=-armSwing;p.leftArm.rotation.z=Math.PI*0.12+Math.sin(p.wobblePhase*0.5)*0.25}
if(p.leftElbow){p.leftElbow.rotation.x=Math.max(0,-armSwing*0.6);p.leftElbow.rotation.z=Math.sin(p.wobblePhase)*0.2}
if(p.rightArm){p.rightArm.rotation.x=armSwing;p.rightArm.rotation.z=-Math.PI*0.12-Math.sin(p.wobblePhase*0.5)*0.25}
if(p.rightElbow){p.rightElbow.rotation.x=Math.max(0,armSwing*0.6);p.rightElbow.rotation.z=-Math.sin(p.wobblePhase)*0.2}
if(p.leftLeg)p.leftLeg.rotation.x=legSwing;if(p.leftKnee)p.leftKnee.rotation.x=(legSwing<0)?-legSwing*0.9:0.1;if(p.rightLeg)p.rightLeg.rotation.x=-legSwing;if(p.rightKnee)p.rightKnee.rotation.x=(-legSwing<0)?legSwing*0.9:0.1}else{p.wobblePhase+=dt*3.5;const idleS=Math.sin(p.wobblePhase);const idleC=Math.cos(p.wobblePhase*0.8);if(p.pelvisGroup){p.pelvisGroup.position.y=0.5+idleS*0.04;p.pelvisGroup.rotation.z=idleC*0.06}
if(p.spineGroup){p.spineGroup.rotation.x=idleS*0.08;p.spineGroup.rotation.z=-idleC*0.06}
if(p.headPivot){p.headPivot.rotation.x=-idleS*0.1;p.headPivot.rotation.z=idleC*0.09}
if(p.leftArm){p.leftArm.rotation.x=THREE.MathUtils.lerp(p.leftArm.rotation.x,idleS*0.15,0.2);p.leftArm.rotation.z=THREE.MathUtils.lerp(p.leftArm.rotation.z,0.2+idleC*0.1,0.2)}
if(p.leftElbow)p.leftElbow.rotation.x=0.15+idleS*0.1;if(p.rightArm){p.rightArm.rotation.x=THREE.MathUtils.lerp(p.rightArm.rotation.x,-idleS*0.15,0.2);p.rightArm.rotation.z=THREE.MathUtils.lerp(p.rightArm.rotation.z,-0.2-idleC*0.1,0.2)}
if(p.rightElbow)p.rightElbow.rotation.x=0.15-idleS*0.1;if(p.leftLeg)p.leftLeg.rotation.x=THREE.MathUtils.lerp(p.leftLeg.rotation.x,0,0.2);if(p.leftKnee)p.leftKnee.rotation.x=THREE.MathUtils.lerp(p.leftKnee.rotation.x,0.05,0.2);if(p.rightLeg)p.rightLeg.rotation.x=THREE.MathUtils.lerp(p.rightLeg.rotation.x,0,0.2);if(p.rightKnee)p.rightKnee.rotation.x=THREE.MathUtils.lerp(p.rightKnee.rotation.x,0.05,0.2);}
terrainColliders.forEach(function(c){if(c.type==='obstacle'||c.type==='sphere'){const dx=p.pos.x-c.x;const dz=p.pos.z-c.z;const d=Math.hypot(dx,dz);const minDist=c.radius+0.6;if(d<minDist&&d>0.001){const push=minDist-d;p.pos.x+=(dx/d)*push;p.pos.z+=(dz/d)*push}}})}
window.updatePlayerPhysics=updatePlayerPhysics;function triggerPlayerDive(p,input){if(p.isDown||p.isDiving||(p.diveCooldown&&p.diveCooldown>0))return;p.isDiving=!0;p.diveTimer=0.45;p.diveCooldown=3.5;p.invincibleTimer=0.35;if(window.FloppySound&&window.FloppySound.playDive){window.FloppySound.playDive()}
p.squashScale.y=0.65;p.squashScale.vy=-8.0;let kx=0;let kz=0;if(input.keys){if(input.keys.KeyW||input.keys.ArrowUp)kz-=1;if(input.keys.KeyS||input.keys.ArrowDown)kz+=1;if(input.keys.KeyA||input.keys.ArrowLeft)kx-=1;if(input.keys.KeyD||input.keys.ArrowRight)kx+=1}
let mx=(input.moveX!==undefined&&input.moveX!==0)?input.moveX:kx;let mz=(input.moveZ!==undefined&&input.moveZ!==0)?input.moveZ:kz;const inputLen=Math.hypot(mx,mz);const yaw=input.cameraYaw||0;let diveDirX,diveDirZ;if(inputLen>0.15){const normX=mx/inputLen;const normZ=mz/inputLen;diveDirX=normX*Math.cos(yaw)+normZ*Math.sin(yaw);diveDirZ=-normX*Math.sin(yaw)+normZ*Math.cos(yaw)}else{diveDirX=-Math.sin(yaw);diveDirZ=-Math.cos(yaw)}
p.rotationY=Math.atan2(diveDirX,diveDirZ);p.mesh.rotation.y=p.rotationY;p.vel.x=diveDirX*14.0;p.vel.z=diveDirZ*14.0;p.vel.y=2.6;if(p.isMe){const overlay=document.querySelector('#ragdoll-feedback-overlay');const textEl=document.querySelector('#ragdoll-banner-text');if(textEl)textEl.innerHTML='<i class="fa-solid fa-person-running"></i> 緊急ダイブ回避！';if(overlay)overlay.classList.remove('hidden');}}
window.triggerPlayerDive=triggerPlayerDive;function throwSnowball(scene,p,input,chargeSec,snowballs,onThrowPacket){const duration=(typeof chargeSec==='number'&&!isNaN(chargeSec))?chargeSec:0.1;const rate=Math.min(1.0,Math.max(0.0,duration/1.5));const size=0.18+rate*0.42;const speed=24.0-rate*11.0;const damage=20+rate*30;const ballId='ball_'+Date.now()+'_'+Math.random().toString(36).substring(2,6);if(window.FloppySound)window.FloppySound.playThrow(rate);const geo=new THREE.SphereGeometry(size,12,12);const mat=new THREE.MeshStandardMaterial({color:0xffffff,roughness:0.9});const mesh=new THREE.Mesh(geo,mat);mesh.castShadow=!0;let vx,vy,vz;const elevation=0.22;let spawnX,spawnY,spawnZ;if(p.isMe&&input&&input.cameraYaw!==undefined){const yaw=input.cameraYaw;const fwdX=-Math.sin(yaw);const fwdZ=-Math.cos(yaw);spawnX=p.pos.x+fwdX*0.8;spawnY=p.pos.y+1.2;spawnZ=p.pos.z+fwdZ*0.8;vx=fwdX*Math.cos(elevation)*speed;vy=Math.sin(elevation)*speed;vz=fwdZ*Math.cos(elevation)*speed}else{const rotY=p.rotationY||0;const fwdX=Math.sin(rotY);const fwdZ=Math.cos(rotY);spawnX=p.pos.x+fwdX*0.8;spawnY=p.pos.y+1.2;spawnZ=p.pos.z+fwdZ*0.8;vx=fwdX*Math.cos(elevation)*speed;vy=Math.sin(elevation)*speed;vz=fwdZ*Math.cos(elevation)*speed}
mesh.position.set(spawnX,spawnY,spawnZ);scene.add(mesh);snowballs.push({id:ballId,owner:p,isRemote:!1,mesh:mesh,pos:mesh.position,vel:new THREE.Vector3(vx,vy,vz),size:size,damage:damage,life:5.0,isDead:!1});if(onThrowPacket){onThrowPacket({t:5,ballId:ballId,origin:[spawnX,spawnY,spawnZ],velocity:[vx,vy,vz],size:size,damage:damage})}}
window.throwSnowball=throwSnowball;function updateSnowballs(snowballs,players,terrainColliders,dt,particles,page,onHitCallback){for(let i=snowballs.length-1;i>=0;i--){const b=snowballs[i];b.life-=dt;b.vel.y-=14.0*dt;b.pos.addScaledVector(b.vel,dt);let hit=!1;if(b.pos.y<=b.size){hit=!0;spawnHitParticles(particles,b.pos,0xffffff,8)}
if(!hit&&terrainColliders&&terrainColliders.length>0){for(let k=0;k<terrainColliders.length;k++){const col=terrainColliders[k];if(col.type==='obstacle'||col.type==='sphere'){const dx=b.pos.x-col.x;const dz=b.pos.z-col.z;const distXZ=Math.hypot(dx,dz);const hitRadius=(col.radius||2.0)+b.size;const minY=(col.minY!==undefined)?col.minY:-1.0;const maxY=(col.maxY!==undefined)?col.maxY:3.5;if(distXZ<=hitRadius&&b.pos.y>=minY&&b.pos.y<=maxY){hit=!0;spawnHitParticles(particles,b.pos,0xffffff,12);break}}}}
const canDetectHits=!hit&&!b.isRemote&&(b.owner.isMe||b.owner.data.isBot);if(canDetectHits){for(let j=0;j<players.length;j++){const target=players[j];if(target===b.owner||target.isDead)continue;const dist=b.pos.distanceTo(target.pos.clone().add(new THREE.Vector3(0,0.8,0)));if(dist<(b.size+0.75)){if(target.invincibleTimer>0){hit=!0;spawnHitParticles(particles,b.pos,0x38bdf8,12);if(target.isMe&&page&&window.addKillFeed){window.addKillFeed(page,'DODGE!!','雪玉をジャストダイブで回避！ (+15 pt)');target.score=(target.score||0)+15}
break}
hit=!0;const pushStrength=16.0+(100-Math.max(0,target.hp))*0.15;const impulse=b.vel.clone().normalize().multiplyScalar(pushStrength);const impulseArr=[impulse.x,8.5,impulse.z];b.owner.score=(b.owner.score||0)+Math.round(b.damage);if(target.hp-b.damage<=0){b.owner.score=(b.owner.score||0)+100}
target.hp=Math.max(0,target.hp-b.damage);target.invincibleTimer=0.35;target.isDown=!0;target.downTimer=1.4;target.vel.set(impulseArr[0],impulseArr[1],impulseArr[2]);spawnHitParticles(particles,b.pos,0x38bdf8,16);if(window.FloppySound)window.FloppySound.playHit();if(onHitCallback){onHitCallback({targetIdx:target.playerIndex,damage:b.damage,impulse:impulseArr,shooterIdx:b.owner.playerIndex,shooterScore:b.owner.score,targetHp:target.hp})}
break}}}
if(hit||b.life<=0){if(b.mesh.parent)b.mesh.parent.remove(b.mesh);snowballs.splice(i,1)}}}
window.updateSnowballs=updateSnowballs;function updateBotAI(bot,players,dt,colliders,scene,snowballs,healItems,particles,page){if(bot.isDead)return;let onGround=!1;let groundHeight=0;colliders.forEach(function(col){if(col.type==='platform'){const dist=Math.hypot(bot.pos.x-col.centerX,bot.pos.z-col.centerZ);if(dist<=col.radius){onGround=!0;groundHeight=col.topY}}else if(col.type==='bridge'){if(bot.pos.x>=col.minX&&bot.pos.x<=col.maxX&&bot.pos.z>=col.minZ&&bot.pos.z<=col.maxZ){onGround=!0;groundHeight=col.topY}}});const kSquash=120.0;const dSquash=10.0;const fSquash=-kSquash*(bot.squashScale.y-1.0)-dSquash*bot.squashScale.vy;bot.squashScale.vy+=fSquash*dt;bot.squashScale.y+=bot.squashScale.vy*dt;bot.squashScale.xz=1.0/Math.sqrt(Math.max(0.2,bot.squashScale.y));if(bot.squashRoot){bot.squashRoot.scale.set(bot.squashScale.xz,bot.squashScale.y,bot.squashScale.xz)}
if(bot.diveCooldown>0){bot.diveCooldown-=dt}
if(bot.isDiving){bot.diveTimer-=dt;bot.vel.y-=30.0*dt;bot.pos.y+=bot.vel.y*dt;bot.pos.x+=bot.vel.x*dt;bot.pos.z+=bot.vel.z*dt;if(onGround&&bot.pos.y<=groundHeight){bot.pos.y=groundHeight;bot.vel.y=0.0;bot.vel.x*=0.92;bot.vel.z*=0.92}
bot.mesh.rotation.x=THREE.MathUtils.lerp(bot.mesh.rotation.x,Math.PI/2.2,0.25);bot.mesh.rotation.z=THREE.MathUtils.lerp(bot.mesh.rotation.z,0,0.25);if(bot.leftArm){bot.leftArm.rotation.x=-Math.PI*0.8;bot.leftArm.rotation.z=0.2}
if(bot.rightArm){bot.rightArm.rotation.x=-Math.PI*0.8;bot.rightArm.rotation.z=-0.2}
if(bot.diveTimer<=0){bot.isDiving=!1;bot.mesh.rotation.x=0;bot.vel.y=2.5;bot.squashScale.y=1.15;bot.squashScale.vy=-4.0}
return}
if(bot.isDown){bot.downTimer-=dt;bot.vel.y-=32.0*dt;bot.pos.y+=bot.vel.y*dt;bot.pos.x+=bot.vel.x*dt;bot.pos.z+=bot.vel.z*dt;if(onGround&&bot.pos.y<=groundHeight){bot.pos.y=groundHeight;bot.vel.y=-bot.vel.y*0.3;bot.vel.x*=0.85;bot.vel.z*=0.85}else{bot.vel.x*=0.98;bot.vel.z*=0.98}
bot.mesh.rotation.z=THREE.MathUtils.lerp(bot.mesh.rotation.z,Math.PI/2.1,0.2);bot.mesh.rotation.x=THREE.MathUtils.lerp(bot.mesh.rotation.x,Math.PI/6,0.15);if(bot.headPivot){bot.headPivot.rotation.x=Math.sin(Date.now()*0.008)*0.4+0.3;bot.headPivot.rotation.z=Math.cos(Date.now()*0.006)*0.5}
if(bot.pomPivot){bot.pomPivot.position.x=Math.sin(Date.now()*0.015)*0.6;bot.pomPivot.position.z=Math.cos(Date.now()*0.015)*0.6}
if(bot.leftArm)bot.leftArm.rotation.z=Math.PI*0.5+Math.sin(Date.now()*0.01)*0.3;if(bot.leftElbow)bot.leftElbow.rotation.z=Math.PI*0.4+Math.cos(Date.now()*0.012)*0.4;if(bot.rightArm)bot.rightArm.rotation.z=-Math.PI*0.5-Math.sin(Date.now()*0.01)*0.3;if(bot.rightElbow)bot.rightElbow.rotation.z=-Math.PI*0.4-Math.cos(Date.now()*0.012)*0.4;if(bot.leftLeg)bot.leftLeg.rotation.x=Math.PI*0.35+Math.sin(Date.now()*0.008)*0.2;if(bot.leftKnee)bot.leftKnee.rotation.x=-Math.PI*0.5;if(bot.rightLeg)bot.rightLeg.rotation.x=-Math.PI*0.2;if(bot.rightKnee)bot.rightKnee.rotation.x=-Math.PI*0.3;const groundSpeed=Math.hypot(bot.vel.x,bot.vel.z);if(bot.downTimer<=0&&onGround&&bot.pos.y>=groundHeight-0.2&&groundSpeed<2.0){bot.isDown=!1;bot.mesh.rotation.z=0;bot.mesh.rotation.x=0;bot.invincibleTimer=0.35;bot.vel.y=4.2;bot.squashScale.y=1.35;bot.squashScale.vy=-6.0}
return}
if(!bot.aiState){bot.aiState={strafeDir:Math.random()<0.5?1:-1,strafeTimer:0,chargeTimer:0,isCharging:!1,targetChargeTime:0.2,throwCooldown:1.0+Math.random()*1.2}}
const ai=bot.aiState;ai.strafeTimer-=dt;if(ai.strafeTimer<=0){ai.strafeDir*=-1;ai.strafeTimer=1.2+Math.random()*1.5}
let incomingThreat=null;let minThreatDist=Infinity;if(snowballs&&snowballs.length>0){for(let sIdx=0;sIdx<snowballs.length;sIdx++){const b=snowballs[sIdx];if(b.owner===bot||b.isDead)continue;const toBot=bot.pos.clone().sub(b.pos);const bSpeed=b.vel.length();if(bSpeed<1.0)continue;const dot=toBot.clone().normalize().dot(b.vel.clone().normalize());const dist=toBot.length();if(dot>0.82&&dist<16.0&&dist<minThreatDist){minThreatDist=dist;incomingThreat=b}}}
if(incomingThreat&&minThreatDist<6.5&&(!bot.diveCooldown||bot.diveCooldown<=0)){bot.isDiving=!0;bot.diveTimer=0.45;bot.diveCooldown=3.5;bot.invincibleTimer=0.35;if(window.FloppySound&&window.FloppySound.playDive)window.FloppySound.playDive();const dodgeAngle=bot.rotationY+(Math.random()<0.5?Math.PI*0.5:-Math.PI*0.5);bot.rotationY=dodgeAngle;bot.mesh.rotation.y=dodgeAngle;bot.vel.x=Math.sin(dodgeAngle)*14.0;bot.vel.z=Math.cos(dodgeAngle)*14.0;bot.vel.y=2.6;return}
let targetHealItem=null;if(bot.hp<=75&&healItems&&healItems.length>0){let minItemDist=Infinity;healItems.forEach(function(item){if(item.active){const d=bot.pos.distanceTo(item.mesh.position);if(d<minItemDist){minItemDist=d;targetHealItem=item}}})}
let bestTarget=null;let bestScore=-Infinity;players.forEach(function(other){if(other===bot||other.isDead)return;const d=bot.pos.distanceTo(other.pos);const score=(100-other.hp)*1.5-d*2.0;if(score>bestScore){bestScore=score;bestTarget=other}});let moveTargetPos=null;let desiredSpeed=10.0;if(targetHealItem){moveTargetPos=targetHealItem.mesh.position;desiredSpeed=11.5}else if(bestTarget){const distToEnemy=bot.pos.distanceTo(bestTarget.pos);if(distToEnemy<7.0){const awayVec=bot.pos.clone().sub(bestTarget.pos).normalize().multiplyScalar(10.0);moveTargetPos=bot.pos.clone().add(awayVec)}else if(distToEnemy>22.0){moveTargetPos=bestTarget.pos}else{const toEnemy=bestTarget.pos.clone().sub(bot.pos).normalize();const rightVec=new THREE.Vector3(-toEnemy.z,0,toEnemy.x).multiplyScalar(ai.strafeDir*8.0);moveTargetPos=bot.pos.clone().add(rightVec)}}
const distFromCenter=Math.hypot(bot.pos.x,bot.pos.z);if(distFromCenter>38.0){const centerVec=new THREE.Vector3(0,0,0).sub(bot.pos).normalize().multiplyScalar(15.0);moveTargetPos=bot.pos.clone().add(centerVec);desiredSpeed=12.5}
const prevVx=bot.vel.x;const prevVz=bot.vel.z;if(moveTargetPos){const moveDir=moveTargetPos.clone().sub(bot.pos);moveDir.y=0;if(moveDir.length()>0.1){moveDir.normalize();bot.vel.x=THREE.MathUtils.lerp(bot.vel.x,moveDir.x*desiredSpeed,0.18);bot.vel.z=THREE.MathUtils.lerp(bot.vel.z,moveDir.z*desiredSpeed,0.18)}}
const ax=(bot.vel.x-prevVx)/Math.max(0.001,dt);const az=(bot.vel.z-prevVz)/Math.max(0.001,dt);if(bestTarget){const distToEnemy=bot.pos.distanceTo(bestTarget.pos);const estBulletSpeed=18.0;const travelTime=Math.max(0.1,distToEnemy/estBulletSpeed);const predictedEnemyPos=bestTarget.pos.clone().add(bestTarget.vel.clone().multiplyScalar(travelTime));const aimDir=predictedEnemyPos.sub(bot.pos);const targetAngle=Math.atan2(aimDir.x,aimDir.z);bot.rotationY=THREE.MathUtils.lerp(bot.rotationY,targetAngle,0.25);bot.mesh.rotation.y=bot.rotationY;ai.throwCooldown-=dt;if(ai.throwCooldown<=0&&distToEnemy<32.0){if(!ai.isCharging){ai.isCharging=!0;ai.targetChargeTime=distToEnemy>16.0?(0.6+Math.random()*0.7):(0.1+Math.random()*0.3);ai.chargeTimer=0}else{ai.chargeTimer+=dt;if(ai.chargeTimer>=ai.targetChargeTime){ai.isCharging=!1;ai.throwCooldown=1.6+Math.random()*1.4;throwSnowball(scene,bot,null,ai.chargeTimer,snowballs)}}}}
const cosR=Math.cos(bot.rotationY);const sinR=Math.sin(bot.rotationY);const localAx=ax*cosR-az*sinR;const localAz=ax*sinR+az*cosR;const groundSpeed=Math.hypot(bot.vel.x,bot.vel.z);const kSpine=80.0;const dSpine=8.5;const targetSpinePitch=-localAz*0.025+(groundSpeed>0.5?0.18:0);const targetSpineRoll=localAx*0.03;const fSpinePitch=-kSpine*(bot.spineSpring.pitch-targetSpinePitch)-dSpine*bot.spineSpring.vPitch;const fSpineRoll=-kSpine*(bot.spineSpring.roll-targetSpineRoll)-dSpine*bot.spineSpring.vRoll;bot.spineSpring.vPitch+=fSpinePitch*dt;bot.spineSpring.vRoll+=fSpineRoll*dt;bot.spineSpring.pitch+=bot.spineSpring.vPitch*dt;bot.spineSpring.roll+=bot.spineSpring.vRoll*dt;const kHead=65.0;const dHead=7.0;const targetHeadPitch=bot.spineSpring.pitch*1.3-localAz*0.015;const targetHeadRoll=-bot.spineSpring.roll*1.2;const fHeadPitch=-kHead*(bot.headSpring.pitch-targetHeadPitch)-dHead*bot.headSpring.vPitch;const fHeadRoll=-kHead*(bot.headSpring.roll-targetHeadRoll)-dHead*bot.headSpring.vRoll;bot.headSpring.vPitch+=fHeadPitch*dt;bot.headSpring.vRoll+=fHeadRoll*dt;bot.headSpring.pitch+=bot.headSpring.vPitch*dt;bot.headSpring.roll+=bot.headSpring.vRoll*dt;const kPom=140.0;const dPom=6.0;bot.pomSpring.vx+=(-kPom*bot.pomSpring.x-dPom*bot.pomSpring.vx-localAx*0.1)*dt;bot.pomSpring.vz+=(-kPom*bot.pomSpring.z-dPom*bot.pomSpring.vz-localAz*0.1)*dt;bot.pomSpring.x+=bot.pomSpring.vx*dt;bot.pomSpring.z+=bot.pomSpring.vz*dt;if(bot.pomPivot){bot.pomPivot.position.x=THREE.MathUtils.clamp(bot.pomSpring.x,-0.3,0.3);bot.pomPivot.position.z=THREE.MathUtils.clamp(bot.pomSpring.z,-0.3,0.3);bot.pomPivot.rotation.z=-bot.pomSpring.x*2.0;bot.pomPivot.rotation.x=bot.pomSpring.z*2.0}
if(bot.scarfTailPivot){bot.scarfTailPivot.rotation.x=THREE.MathUtils.lerp(bot.scarfTailPivot.rotation.x,Math.PI*0.15+groundSpeed*0.08+Math.sin(bot.wobblePhase*2)*0.2,0.2);bot.scarfTailPivot.rotation.z=Math.cos(bot.wobblePhase)*0.2}
if(bot.bellyMesh){const bellyWobble=Math.sin(bot.wobblePhase*1.5)*0.12;bot.bellyMesh.scale.set(0.9+bellyWobble,0.95-bellyWobble,0.6+bellyWobble*1.2)}
if(groundSpeed>0.3){bot.wobblePhase+=dt*(12.0+groundSpeed*0.6);const legS=Math.sin(bot.wobblePhase)*0.85;const armS=Math.cos(bot.wobblePhase)*0.95;const bounceY=Math.abs(Math.sin(bot.wobblePhase))*0.12;if(bot.pelvisGroup){bot.pelvisGroup.position.y=0.5+bounceY;bot.pelvisGroup.rotation.z=Math.sin(bot.wobblePhase)*0.12;bot.pelvisGroup.rotation.y=Math.cos(bot.wobblePhase)*0.15}
if(bot.spineGroup){bot.spineGroup.rotation.x=bot.spineSpring.pitch;bot.spineGroup.rotation.z=bot.spineSpring.roll+Math.sin(bot.wobblePhase)*0.08}
if(bot.headPivot){bot.headPivot.rotation.x=bot.headSpring.pitch;bot.headPivot.rotation.z=bot.headSpring.roll+Math.cos(bot.wobblePhase)*0.1}
if(bot.leftArm){bot.leftArm.rotation.x=-armS;bot.leftArm.rotation.z=Math.PI*0.12+Math.sin(bot.wobblePhase*0.5)*0.25}
if(bot.leftElbow){bot.leftElbow.rotation.x=Math.max(0,-armS*0.6);bot.leftElbow.rotation.z=Math.sin(bot.wobblePhase)*0.2}
if(bot.rightArm){bot.rightArm.rotation.x=armS;bot.rightArm.rotation.z=-Math.PI*0.12-Math.sin(bot.wobblePhase*0.5)*0.25}
if(bot.rightElbow){bot.rightElbow.rotation.x=Math.max(0,armS*0.6);bot.rightElbow.rotation.z=-Math.sin(bot.wobblePhase)*0.2}
if(bot.leftLeg)bot.leftLeg.rotation.x=legS;if(bot.leftKnee)bot.leftKnee.rotation.x=(legS<0)?-legS*0.9:0.1;if(bot.rightLeg)bot.rightLeg.rotation.x=-legS;if(bot.rightKnee)bot.rightKnee.rotation.x=(-legS<0)?legS*0.9:0.1}else{bot.wobblePhase+=dt*3.5;const idleS=Math.sin(bot.wobblePhase);const idleC=Math.cos(bot.wobblePhase*0.8);if(bot.pelvisGroup){bot.pelvisGroup.position.y=0.5+idleS*0.04;bot.pelvisGroup.rotation.z=idleC*0.06}
if(bot.spineGroup){bot.spineGroup.rotation.x=idleS*0.08;bot.spineGroup.rotation.z=-idleC*0.06}
if(bot.headPivot){bot.headPivot.rotation.x=-idleS*0.1;bot.headPivot.rotation.z=idleC*0.09}
if(bot.leftArm){bot.leftArm.rotation.x=THREE.MathUtils.lerp(bot.leftArm.rotation.x,idleS*0.15,0.2);bot.leftArm.rotation.z=THREE.MathUtils.lerp(bot.leftArm.rotation.z,0.2+idleC*0.1,0.2)}
if(bot.leftElbow)bot.leftElbow.rotation.x=0.15+idleS*0.1;if(bot.rightArm){bot.rightArm.rotation.x=THREE.MathUtils.lerp(bot.rightArm.rotation.x,-idleS*0.15,0.2);bot.rightArm.rotation.z=THREE.MathUtils.lerp(bot.rightArm.rotation.z,-0.2-idleC*0.1,0.2)}
if(bot.rightElbow)bot.rightElbow.rotation.x=0.15-idleS*0.1;if(bot.leftLeg)bot.leftLeg.rotation.x=THREE.MathUtils.lerp(bot.leftLeg.rotation.x,0,0.2);if(bot.leftKnee)bot.leftKnee.rotation.x=THREE.MathUtils.lerp(bot.leftKnee.rotation.x,0.05,0.2);if(bot.rightLeg)bot.rightLeg.rotation.x=THREE.MathUtils.lerp(bot.rightLeg.rotation.x,0,0.2);if(bot.rightKnee)bot.rightKnee.rotation.x=THREE.MathUtils.lerp(bot.rightKnee.rotation.x,0.05,0.2);}
bot.vel.y-=32.0*dt;const prevY=bot.pos.y;bot.pos.y+=bot.vel.y*dt;if(onGround&&bot.pos.y<=groundHeight){if(prevY>groundHeight+0.1){bot.squashScale.y=0.72;bot.squashScale.vy=7.0}
bot.pos.y=groundHeight;bot.vel.y=0.0}
bot.pos.x+=bot.vel.x*dt;bot.pos.z+=bot.vel.z*dt;colliders.forEach(function(c){if(c.type==='obstacle'||c.type==='sphere'){const dx=bot.pos.x-c.x;const dz=bot.pos.z-c.z;const d=Math.hypot(dx,dz);const minDist=c.radius+0.6;if(d<minDist&&d>0.001){const push=minDist-d;bot.pos.x+=(dx/d)*push;bot.pos.z+=(dz/d)*push}}})}
window.updateBotAI=updateBotAI;function createParticleSystem(scene){const maxParticles=120;const geo=new THREE.BufferGeometry();const positions=new Float32Array(maxParticles*3);const colors=new Float32Array(maxParticles*3);geo.setAttribute('position',new THREE.BufferAttribute(positions,3));geo.setAttribute('color',new THREE.BufferAttribute(colors,3));const mat=new THREE.PointsMaterial({size:0.25,vertexColors:!0,transparent:!0,opacity:0.85});const points=new THREE.Points(geo,mat);scene.add(points);return{mesh:points,positions:positions,colors:colors,velocities:new Float32Array(maxParticles*3),lifes:new Float32Array(maxParticles),max:maxParticles,nextIdx:0}}
window.createParticleSystem=createParticleSystem;function spawnHitParticles(pSystem,pos,colorHex,count){const col=new THREE.Color(colorHex);for(let i=0;i<count;i++){const idx=pSystem.nextIdx;pSystem.nextIdx=(pSystem.nextIdx+1)%pSystem.max;const i3=idx*3;pSystem.positions[i3]=pos.x;pSystem.positions[i3+1]=pos.y;pSystem.positions[i3+2]=pos.z;pSystem.colors[i3]=col.r;pSystem.colors[i3+1]=col.g;pSystem.colors[i3+2]=col.b;pSystem.velocities[i3]=(Math.random()-0.5)*8.0;pSystem.velocities[i3+1]=Math.random()*6.0+2.0;pSystem.velocities[i3+2]=(Math.random()-0.5)*8.0;pSystem.lifes[idx]=0.6+Math.random()*0.4}}
window.spawnHitParticles=spawnHitParticles;function updateParticles(pSystem,dt){let dirty=!1;for(let i=0;i<pSystem.max;i++){if(pSystem.lifes[i]>0){pSystem.lifes[i]-=dt;const i3=i*3;pSystem.velocities[i3+1]-=15.0*dt;pSystem.positions[i3]+=pSystem.velocities[i3]*dt;pSystem.positions[i3+1]+=pSystem.velocities[i3+1]*dt;pSystem.positions[i3+2]+=pSystem.velocities[i3+2]*dt;dirty=!0}}
if(dirty){pSystem.mesh.geometry.attributes.position.needsUpdate=!0}}
window.updateParticles=updateParticles;function updateFollowCamera(camera,player,input){const yaw=input.cameraYaw||0;const pitch=input.cameraPitch||0.25;const camDist=6.5;const targetPos=player.pos.clone().add(new THREE.Vector3(0,1.2,0));const offsetX=Math.sin(yaw)*Math.cos(pitch)*camDist;const offsetY=Math.sin(pitch)*camDist+1.2;const offsetZ=Math.cos(yaw)*Math.cos(pitch)*camDist;const desiredCamPos=targetPos.clone().add(new THREE.Vector3(offsetX,offsetY,offsetZ));camera.position.lerp(desiredCamPos,0.12);camera.lookAt(targetPos)}
window.updateFollowCamera=updateFollowCamera;function endGame(page,players){if(window.FloppySound)window.FloppySound.playWhistle();if(document.exitPointerLock){try{document.exitPointerLock()}catch(e){}}
const modal=page.querySelector('#game-result-modal');const board=page.querySelector('#result-leaderboard');const title=page.querySelector('#result-winner-title');if(!modal||!board)return;const sorted=players.slice().sort(function(a,b){if(b.score!==a.score)return b.score-a.score;return(b.isDead?0:1)-(a.isDead?0:1)});const winner=sorted[0];if(title){title.textContent=winner?(winner.data.name+' の勝利！🏆'):'TIME UP!'}
let html='';sorted.forEach(function(p,rank){const rankClass='rank-'+(rank+1);html+='<div class="result-row '+rankClass+'">'+'<div class="result-left">'+'<span class="rank-num">#'+(rank+1)+'</span>'+'<span class="result-pname">'+p.data.name+'</span>'+'</div>'+'<div class="result-score-val">'+(p.score||0)+' pt</div>'+'</div>'});board.innerHTML=html;if(window.FloppyState&&window.FloppyState.isBotMatch){setTimeout(function(){const botPhrases=['対戦ありがとう！🎉','ナイスファイト！⛄','楽しかった〜！❄️','また遊ぼう！👋'];const botPlayers=sorted.filter(function(p){return p.data&&p.data.isBot});if(botPlayers.length>0){const randBot=botPlayers[Math.floor(Math.random()*botPlayers.length)];const randPhrase=botPhrases[Math.floor(Math.random()*botPhrases.length)];if(window.appendResultChatBubble){window.appendResultChatBubble(randBot.data.name,randPhrase,!1)}}},1000)}
modal.classList.remove('hidden')}
window.endGame=endGame;function handleGameLeave(){if(document.exitPointerLock){try{document.exitPointerLock()}catch(e){}}
if(window.FloppySound&&window.FloppySound.playWhistle){window.FloppySound.playWhistle()}
const page=document.querySelector('#game-page');if(page&&page._cleanupGame){page._cleanupGame()}
if(window.cleanupHostRoom){window.cleanupHostRoom()}
window._cachedDiscoveredRooms={};const nav=document.querySelector('#appNavigator');if(nav){nav.resetToPage('home.html')}}
window.handleGameLeave=handleGameLeave;function checkAndAutoStartIfReady(room){if(!room||!room.players||room.players.length<2)return;const nonHostPlayers=room.players.filter(function(p){return!p.isHost});const allGuestsReady=nonHostPlayers.length>0&&nonHostPlayers.every(function(p){return p.isReady});if(allGuestsReady){const log=document.querySelector('#room-chat-log');if(log){const alertDiv=document.createElement('div');alertDiv.className='system-msg';alertDiv.style.color='#38bdf8';alertDiv.style.fontWeight='bold';alertDiv.innerHTML='<i class="fa-solid fa-bolt"></i> 参加者全員の準備が完了しました！まもなく試合を開始します...';log.appendChild(alertDiv);log.scrollTop=log.scrollHeight}
if(!room._autoStartTimerId){room._autoStartTimerId=setTimeout(function(){room._autoStartTimerId=null;if(window.handleRoomStartGame){window.handleRoomStartGame()}},1500)}}else{if(room._autoStartTimerId){clearTimeout(room._autoStartTimerId);room._autoStartTimerId=null}}}
window.checkAndAutoStartIfReady=checkAndAutoStartIfReady;function spawnHealItem(scene,healItems,x,y,z){const group=new THREE.Group();const geo=new THREE.OctahedronGeometry(0.7,0);const mat=new THREE.MeshStandardMaterial({color:0x10b981,emissive:0x059669,roughness:0.1,metalness:0.8});const mesh=new THREE.Mesh(geo,mat);mesh.position.y=1.0;mesh.castShadow=!0;group.add(mesh);group.position.set(x,y,z);scene.add(group);healItems.push({mesh:group,active:!0,respawnTimer:0})}
window.spawnHealItem=spawnHealItem;function addKillFeed(page,title,desc){const container=page.querySelector('#hud-kill-feed');if(!container)return;const item=document.createElement('div');item.className='kill-feed-item';item.innerHTML='<span style="color:#f43f5e; font-weight:800;">'+title+'</span> '+desc;container.appendChild(item);setTimeout(function(){item.style.opacity='0';item.style.transform='translateY(-10px)';item.style.transition='opacity 0.3s, transform 0.3s';setTimeout(function(){if(item.parentNode)item.parentNode.removeChild(item);},300)},3500)}
window.addKillFeed=addKillFeed;function createConfettiSystem(scene){const maxParticles=200;const geo=new THREE.BufferGeometry();const positions=new Float32Array(maxParticles*3);const colors=new Float32Array(maxParticles*3);geo.setAttribute('position',new THREE.BufferAttribute(positions,3));geo.setAttribute('color',new THREE.BufferAttribute(colors,3));const mat=new THREE.PointsMaterial({size:0.45,vertexColors:!0,transparent:!0,opacity:0.95});const points=new THREE.Points(geo,mat);scene.add(points);return{mesh:points,positions:positions,colors:colors,velocities:new Float32Array(maxParticles*3),lifes:new Float32Array(maxParticles),max:maxParticles,nextIdx:0}}
window.createConfettiSystem=createConfettiSystem;function spawnWinnerConfetti(cSystem,pos,count){const palette=[0xfbbf24,0xf43f5e,0x38bdf8,0x10b981,0xa855f7,0xffffff];for(let i=0;i<count;i++){const idx=cSystem.nextIdx;cSystem.nextIdx=(cSystem.nextIdx+1)%cSystem.max;const i3=idx*3;cSystem.positions[i3]=pos.x+(Math.random()-0.5)*6.0;cSystem.positions[i3+1]=pos.y+3.0+Math.random()*4.0;cSystem.positions[i3+2]=pos.z+(Math.random()-0.5)*6.0;const col=new THREE.Color(palette[Math.floor(Math.random()*palette.length)]);cSystem.colors[i3]=col.r;cSystem.colors[i3+1]=col.g;cSystem.colors[i3+2]=col.b;cSystem.velocities[i3]=(Math.random()-0.5)*10.0;cSystem.velocities[i3+1]=Math.random()*12.0+4.0;cSystem.velocities[i3+2]=(Math.random()-0.5)*10.0;cSystem.lifes[idx]=2.5+Math.random()*1.0}}
window.spawnWinnerConfetti=spawnWinnerConfetti;function updateConfetti(cSystem,dt){let dirty=!1;for(let i=0;i<cSystem.max;i++){if(cSystem.lifes[i]>0){cSystem.lifes[i]-=dt;const i3=i*3;cSystem.velocities[i3+1]-=8.0*dt;cSystem.positions[i3]+=(cSystem.velocities[i3]+Math.sin(cSystem.lifes[i]*8)*1.5)*dt;cSystem.positions[i3+1]+=cSystem.velocities[i3+1]*dt;cSystem.positions[i3+2]+=(cSystem.velocities[i3+2]+Math.cos(cSystem.lifes[i]*8)*1.5)*dt;dirty=!0}}
if(dirty){cSystem.mesh.geometry.attributes.position.needsUpdate=!0}}
window.updateConfetti=updateConfetti;function cleanupHostRoom(){if(window._roomHeartbeatTimer){clearInterval(window._roomHeartbeatTimer);window._roomHeartbeatTimer=null}
const room=window.FloppyState?window.FloppyState.activeRoom:null;const slotId=(room&&room.slotId)?room.slotId:null;try{const roomsMap=JSON.parse(localStorage.getItem('floppy_active_public_rooms')||'{}');if(slotId&&roomsMap[slotId]){delete roomsMap[slotId]}
const now=Date.now();Object.keys(roomsMap).forEach(function(k){if(now-(roomsMap[k].lastHeartbeat||0)>8000){delete roomsMap[k]}});localStorage.setItem('floppy_active_public_rooms',JSON.stringify(roomsMap))}catch(e){}
if(slotId){try{if(window._floppyRoomChannel){window._floppyRoomChannel.postMessage({type:'ROOM_CLOSED',slotId:slotId})}}catch(e){}}
window._cachedDiscoveredRooms={};if(window.FloppyState){if(window.FloppyState.peer){try{window.FloppyState.peer.destroy()}catch(e){}
window.FloppyState.peer=null}
window.FloppyState.activeRoom=null;window.FloppyState.currentGameSettings=null;window.FloppyState.isHost=!1;window.FloppyState.connections={};window.FloppyState.hostConn=null}}
window.cleanupHostRoom=cleanupHostRoom;function spawnRemoteSnowball(scene,thrower,packet,snowballs){if(!packet)return;const size=packet.size||0.25;const geo=new THREE.SphereGeometry(size,12,12);const mat=new THREE.MeshStandardMaterial({color:0xffffff,roughness:0.9});const mesh=new THREE.Mesh(geo,mat);mesh.castShadow=!0;const origin=packet.origin||[thrower.pos.x,thrower.pos.y+1.2,thrower.pos.z];mesh.position.set(origin[0],origin[1],origin[2]);scene.add(mesh);const vel=packet.velocity||[0,5,0];snowballs.push({id:packet.ballId||('remote_'+Date.now()),owner:thrower,isRemote:!0,mesh:mesh,pos:mesh.position,vel:new THREE.Vector3(vel[0],vel[1],vel[2]),size:size,damage:packet.damage||25,life:5.0,isDead:!1});if(window.FloppySound)window.FloppySound.playThrow(0.5);}
window.spawnRemoteSnowball=spawnRemoteSnowball;function createAndHostRoom(config,retrySlotIndex){const slotIdx=retrySlotIndex||1;const slotId=(config&&config.roomType==='private')?('floppy-priv-'+(config.passcode||'secret')):('floppy-pub-slot-'+(slotIdx<10?'0'+slotIdx:slotIdx));const roomType=(config&&config.roomType)||'public';const passcode=(config&&config.passcode)||'';const stageId=(config&&config.stageId)||'lake';const maxPlayers=(config&&config.maxPlayers)||4;const matchTimeSec=(config&&config.matchTimeSec!==undefined)?config.matchTimeSec:120;const allowLateJoin=(config&&config.allowLateJoin!==undefined)?config.allowLateJoin:!1;const myPlayerName=(window.FloppyState&&window.FloppyState.playerName)||'ゆきんこ';const myPlayerColor=(window.FloppyState&&window.FloppyState.playerColor)||'#38bdf8';window.FloppyState.isHost=!0;window.FloppyState.isBotMatch=!1;window.FloppyState.connections={};window.FloppyState.activeRoom={slotId:slotId,hostName:myPlayerName,stageId:stageId,maxPlayers:maxPlayers,matchTimeSec:matchTimeSec,allowLateJoin:allowLateJoin,roomType:roomType,passcode:passcode,isPlaying:!1,players:[{id:'host-me',name:myPlayerName,color:myPlayerColor,isHost:!0,isReady:!0}]};function goToRoomPage(){const nav=document.querySelector('#appNavigator');if(nav&&(!nav.topPage||nav.topPage.id!=='room-page')){nav.pushPage('room.html')}}
if(window.FloppyState.peer){try{window.FloppyState.peer.destroy()}catch(e){}
window.FloppyState.peer=null}
if(roomType==='public'){try{const roomsMap=JSON.parse(localStorage.getItem('floppy_active_public_rooms')||'{}');roomsMap[slotId]={slotId:slotId,hostName:myPlayerName,stageId:stageId,maxPlayers:maxPlayers,currentPlayers:1,matchTimeSec:matchTimeSec,isPlaying:!1,lastHeartbeat:Date.now()};localStorage.setItem('floppy_active_public_rooms',JSON.stringify(roomsMap))}catch(e){}}
if(typeof Peer!=='undefined'){try{const hostPeer=new Peer(slotId,{debug:0,config:{iceServers:[{urls:'stun:stun.l.google.com:19302'}]}});let isResolved=!1;hostPeer.on('open',function(id){isResolved=!0;window.FloppyState.myPeerId=id;goToRoomPage()});hostPeer.on('connection',function(conn){conn.on('open',function(){window.FloppyState.connections[conn.peer]=conn});conn.on('data',function(data){if(!data)return;const curRoom=window.FloppyState.activeRoom;if(data.t===0||(data.d&&data.d.query==='status')){if(curRoom){conn.send({t:1,d:{hostName:curRoom.hostName,stageId:curRoom.stageId,maxPlayers:curRoom.maxPlayers,currentPlayers:(curRoom.players&&curRoom.players.length)?curRoom.players.length:1,matchTimeSec:curRoom.matchTimeSec,isPlaying:!!curRoom.isPlaying}})}}else if(data.t===2||data.type==='JOIN_REQUEST'){const req=data.d||data;if(curRoom&&curRoom.isPlaying){conn.send({t:3,d:{accepted:!1,reason:'現在対戦中です'}});setTimeout(function(){try{conn.close()}catch(err){}},500);return}
if(curRoom&&curRoom.players.length<curRoom.maxPlayers){const newGuest={id:conn.peer,name:req.playerName||'ゲスト',color:req.playerColor||'#f43f5e',isHost:!1,isReady:!1};curRoom.players.push(newGuest);conn.send({t:3,d:{accepted:!0,room:curRoom}});Object.values(window.FloppyState.connections).forEach(function(c){try{c.send({type:'ROOM_UPDATE',room:curRoom})}catch(err){}});const curPage=document.querySelector('#room-page');if(curPage&&window.initRoomPage)window.initRoomPage({target:curPage})}else{conn.send({t:3,d:{accepted:!1,reason:'満員です'}});setTimeout(function(){try{conn.close()}catch(err){}},500)}}else if(data.type==='TOGGLE_READY'){if(curRoom&&!curRoom.isPlaying){const target=curRoom.players.find(function(p){return p.id===conn.peer});if(target){target.isReady=!target.isReady;Object.values(window.FloppyState.connections).forEach(function(c){try{c.send({type:'ROOM_UPDATE',room:curRoom})}catch(err){}});const curPage=document.querySelector('#room-page');if(curPage&&window.initRoomPage)window.initRoomPage({target:curPage});if(window.checkAndAutoStartIfReady)window.checkAndAutoStartIfReady(curRoom);}}}else if(data.type==='CHAT'){if(window.appendRoomChatMsg)window.appendRoomChatMsg(data.sender,data.text);Object.values(window.FloppyState.connections).forEach(function(c){if(c.peer!==conn.peer){try{c.send(data)}catch(err){}}})}});conn.on('close',function(){delete window.FloppyState.connections[conn.peer];const curRoom=window.FloppyState.activeRoom;if(curRoom&&!curRoom.isPlaying){curRoom.players=curRoom.players.filter(function(p){return p.id!==conn.peer});Object.values(window.FloppyState.connections).forEach(function(c){try{c.send({type:'ROOM_UPDATE',room:curRoom})}catch(err){}});const curPage=document.querySelector('#room-page');if(curPage&&window.initRoomPage)window.initRoomPage({target:curPage})}})});hostPeer.on('error',function(err){if(!isResolved&&err&&(err.type==='unavailable-id'||(err.message&&err.message.indexOf('taken')!==-1))){isResolved=!0;try{hostPeer.destroy()}catch(e){}
window.FloppyState.peer=null;if(slotIdx<8){createAndHostRoom(config,slotIdx+1)}else{goToRoomPage()}}else if(!isResolved){isResolved=!0;goToRoomPage()}});window.FloppyState.peer=hostPeer;return}catch(e){}}
goToRoomPage()}
window.createAndHostRoom=createAndHostRoom;function appendRoomChatMsg(sender,text){const log=document.querySelector('#room-chat-log');if(!log||!text)return;const item=document.createElement('div');item.className='chat-msg-item';item.innerHTML='<span class="chat-sender">'+(sender||'プレイヤー')+':</span> '+text;log.appendChild(item);log.scrollTop=log.scrollHeight}
window.appendRoomChatMsg=appendRoomChatMsg;function sendAndBroadcastChat(text){if(!text)return;const sender=(window.FloppyState&&window.FloppyState.playerName)?window.FloppyState.playerName:'プレイヤー';const myId=(window.FloppyState&&(window.FloppyState.myPeerId||(window.FloppyState.peer&&window.FloppyState.peer.id)))||'';if(window.appendRoomChatMsg){window.appendRoomChatMsg(sender,text)}
const packet={type:'CHAT',s:myId,sender:sender,text:text};if(window.FloppyState&&window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){try{conn.send(packet)}catch(e){}})}else if(window.FloppyState&&window.FloppyState.hostConn){try{window.FloppyState.hostConn.send(packet)}catch(e){}}
try{if(window._floppyRoomChannel){window._floppyRoomChannel.postMessage(packet)}}catch(e){}}
window.sendAndBroadcastChat=sendAndBroadcastChat;function handleStickerClick(event){if(!event||!event.currentTarget)return;const stickerText=event.currentTarget.getAttribute('data-sticker');if(stickerText&&window.sendAndBroadcastChat){window.sendAndBroadcastChat(stickerText)}}
window.handleStickerClick=handleStickerClick;function appendResultChatBubble(sender,text,isMine){const log=document.querySelector('#result-chat-log');if(!log||!text)return;const hint=log.querySelector('#result-chat-hint');if(hint)hint.remove();const bubble=document.createElement('div');bubble.className='result-chat-bubble'+(isMine?' mine':'');const senderSpan=document.createElement('span');senderSpan.className='result-chat-sender';senderSpan.textContent=(sender||'プレイヤー')+':';const textSpan=document.createElement('span');textSpan.className='result-chat-text';textSpan.textContent=text;bubble.appendChild(senderSpan);bubble.appendChild(textSpan);log.appendChild(bubble);log.scrollTop=log.scrollHeight}
window.appendResultChatBubble=appendResultChatBubble;function sendResultChatMessage(text){if(!text)return;const sender=(window.FloppyState&&window.FloppyState.playerName)?window.FloppyState.playerName:'あなた';const myId=(window.FloppyState&&(window.FloppyState.myPeerId||(window.FloppyState.peer&&window.FloppyState.peer.id)))||'';if(window.appendResultChatBubble){window.appendResultChatBubble(sender,text,!0)}
if(window.FloppySound&&window.FloppySound.playGetUp){window.FloppySound.playGetUp()}
const packet={type:'RESULT_CHAT',s:myId,sender:sender,text:text,ts:Date.now()};if(window.FloppyState&&window.FloppyState.isHost&&window.FloppyState.connections){Object.values(window.FloppyState.connections).forEach(function(conn){try{conn.send(packet)}catch(e){}})}else if(window.FloppyState&&window.FloppyState.hostConn){try{window.FloppyState.hostConn.send(packet)}catch(e){}}
try{if(window._floppyRoomChannel){window._floppyRoomChannel.postMessage(packet)}}catch(e){}}
window.sendResultChatMessage=sendResultChatMessage;function handleResultQuickTagClick(event){if(!event||!event.currentTarget)return;const msg=event.currentTarget.getAttribute('data-msg');if(msg&&window.sendResultChatMessage){window.sendResultChatMessage(msg)}}
window.handleResultQuickTagClick=handleResultQuickTagClick;function setupHomeCharacterPreview(page){const container=page.querySelector('#home-character-canvas-box');if(!container||typeof THREE==='undefined')return;if(page._previewAnimId){cancelAnimationFrame(page._previewAnimId);page._previewAnimId=null}
const existingCanvas=container.querySelector('canvas');if(existingCanvas){existingCanvas.remove()}
const width=container.clientWidth||340;const height=Math.max(container.clientHeight||0,240);const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(40,width/height,0.1,50);camera.position.set(0,0.92,3.4);camera.lookAt(0,0.85,0);const renderer=new THREE.WebGLRenderer({antialias:!0,alpha:!0,powerPreference:'high-performance'});renderer.setSize(width,height);renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));renderer.shadowMap.enabled=!0;container.appendChild(renderer.domElement);const ambient=new THREE.AmbientLight(0xffffff,0.95);scene.add(ambient);const dirLight=new THREE.DirectionalLight(0x38bdf8,1.1);dirLight.position.set(4,8,6);scene.add(dirLight);const rimLight=new THREE.DirectionalLight(0xfbbf24,0.7);rimLight.position.set(-4,5,-4);scene.add(rimLight);const charData={name:(window.FloppyState&&window.FloppyState.playerName)||'ゆきんこ',color:(window.FloppyState&&window.FloppyState.playerColor)||'#38bdf8'};let charObj=null;if(window.createFloppyCharacter){charObj=window.createFloppyCharacter(scene,charData,0,0,0);if(charObj&&charObj.mesh){charObj.mesh.traverse(function(child){if(child.isSprite){child.visible=!1}})}}
const previewSnowballs=[];let previewParticles=null;if(window.createParticleSystem){previewParticles=window.createParticleSystem(scene)}
window._updatePreviewCharacterColor=function(newColorHex){if(!charObj||!charObj.mesh)return;const targetCol=new THREE.Color(newColorHex);charObj.mesh.traverse(function(child){if(child.isMesh&&child.material&&child.material.color){if(child.material.roughness===0.5&&child.material.metalness===0.1){child.material.color.copy(targetCol)}}})};window._updatePreviewNameTag=function(newName){};let currentAction='idle';let actionTimer=0;let nextActionDelay=3.5+Math.random()*2.5;function performAction(actionType){if(!charObj)return;currentAction=actionType;actionTimer=0;if(actionType==='throw'){if(window.FloppySound&&window.FloppySound.playThrow){window.FloppySound.playThrow(0.35)}
const ballGeo=new THREE.SphereGeometry(0.18,12,12);const ballMat=new THREE.MeshStandardMaterial({color:0xffffff,roughness:0.8});const ballMesh=new THREE.Mesh(ballGeo,ballMat);ballMesh.position.set(0,0.85,0.4);scene.add(ballMesh);previewSnowballs.push({mesh:ballMesh,vel:new THREE.Vector3((Math.random()-0.5)*0.4,0.8,2.6),life:1.2})}else if(actionType==='dive'){if(window.FloppySound&&window.FloppySound.playDive){window.FloppySound.playDive()}}else if(actionType==='jump'){if(window.FloppySound&&window.FloppySound.playGetUp){window.FloppySound.playGetUp()}}}
let phase=0;let isRunning=!0;let lastTime=performance.now();function animatePreview(now){if(!isRunning)return;page._previewAnimId=requestAnimationFrame(animatePreview);const dt=Math.min((now-lastTime)/1000,0.05);lastTime=now;phase+=0.022;if(currentAction==='idle'){nextActionDelay-=dt;if(nextActionDelay<=0){const pool=['throw','dive','wave','jump'];const choice=pool[Math.floor(Math.random()*pool.length)];performAction(choice);nextActionDelay=4.0+Math.random()*3.5}}
if(charObj&&charObj.mesh){if(currentAction==='throw'){actionTimer+=dt;charObj.mesh.rotation.y=THREE.MathUtils.lerp(charObj.mesh.rotation.y,0,0.25);if(actionTimer<0.22){if(charObj.rightArm)charObj.rightArm.rotation.x=-Math.PI*0.9;if(charObj.leftArm)charObj.leftArm.rotation.x=Math.PI*0.4;if(charObj.spineGroup)charObj.spineGroup.rotation.x=-0.2}else if(actionTimer<0.6){if(charObj.rightArm)charObj.rightArm.rotation.x=Math.PI*0.7;if(charObj.leftArm)charObj.leftArm.rotation.x=-Math.PI*0.3;if(charObj.spineGroup)charObj.spineGroup.rotation.x=0.3}else{currentAction='idle'}}else if(currentAction==='dive'){actionTimer+=dt;if(actionTimer<0.55){charObj.mesh.rotation.y=THREE.MathUtils.lerp(charObj.mesh.rotation.y,0,0.2);charObj.mesh.rotation.x=THREE.MathUtils.lerp(charObj.mesh.rotation.x,Math.PI/2.2,0.25);charObj.pos.y=THREE.MathUtils.lerp(charObj.pos.y,-0.2,0.2);if(charObj.headPivot)charObj.headPivot.rotation.z=Math.PI*0.3}else if(actionTimer<1.2){if(charObj.leftArm)charObj.leftArm.rotation.z=Math.PI*0.5+Math.sin(now*0.01)*0.3;if(charObj.rightArm)charObj.rightArm.rotation.z=-Math.PI*0.5-Math.sin(now*0.01)*0.3}else{if(window.FloppySound&&window.FloppySound.playGetUp){window.FloppySound.playGetUp()}
currentAction='idle'}}else if(currentAction==='wave'){actionTimer+=dt;charObj.mesh.rotation.y=THREE.MathUtils.lerp(charObj.mesh.rotation.y,0,0.2);if(charObj.rightArm){charObj.rightArm.rotation.x=-Math.PI*0.75;charObj.rightArm.rotation.z=-0.5+Math.sin(actionTimer*12)*0.4}
if(charObj.headPivot)charObj.headPivot.rotation.z=Math.sin(actionTimer*6)*0.15;if(actionTimer>1.4)currentAction='idle'}else if(currentAction==='jump'){actionTimer+=dt;charObj.pos.y=Math.abs(Math.sin(actionTimer*4.5))*0.35;if(charObj.leftArm)charObj.leftArm.rotation.z=Math.PI*0.6;if(charObj.rightArm)charObj.rightArm.rotation.z=-Math.PI*0.6;if(actionTimer>0.8){charObj.pos.y=0;currentAction='idle'}}else{charObj.mesh.rotation.x=THREE.MathUtils.lerp(charObj.mesh.rotation.x,0,0.15);charObj.pos.y=THREE.MathUtils.lerp(charObj.pos.y,0,0.15);charObj.mesh.rotation.y=phase*0.85;const s=Math.sin(phase*1.6);const c=Math.cos(phase*1.3);if(charObj.pelvisGroup){charObj.pelvisGroup.position.y=0.5+Math.abs(s)*0.06;charObj.pelvisGroup.rotation.z=c*0.08}
if(charObj.spineGroup){charObj.spineGroup.rotation.x=s*0.08;charObj.spineGroup.rotation.z=-c*0.08}
if(charObj.headPivot){charObj.headPivot.rotation.x=-s*0.1;charObj.headPivot.rotation.z=c*0.12}
if(charObj.pomPivot){charObj.pomPivot.position.x=s*0.08;charObj.pomPivot.position.z=c*0.08}
if(charObj.leftArm){charObj.leftArm.rotation.x=s*0.25;charObj.leftArm.rotation.z=0.35+c*0.12}
if(charObj.rightArm){charObj.rightArm.rotation.x=-s*0.25;charObj.rightArm.rotation.z=-0.35-c*0.12}
if(charObj.leftLeg)charObj.leftLeg.rotation.x=s*0.2;if(charObj.rightLeg)charObj.rightLeg.rotation.x=-s*0.2}}
for(let i=previewSnowballs.length-1;i>=0;i--){const sb=previewSnowballs[i];sb.life-=dt;sb.mesh.position.addScaledVector(sb.vel,dt);sb.vel.y-=2.8*dt;if(sb.life<=0||sb.mesh.position.z>3.0){if(previewParticles&&window.spawnHitParticles){window.spawnHitParticles(previewParticles,sb.mesh.position,0x38bdf8,12)}
scene.remove(sb.mesh);previewSnowballs.splice(i,1)}}
if(previewParticles&&window.updateParticles){window.updateParticles(previewParticles,dt)}
renderer.render(scene,camera)}
requestAnimationFrame(animatePreview);function onResize(){if(!container||!renderer)return;const w=container.clientWidth||340;const h=Math.max(container.clientHeight||0,240);camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h)}
window.addEventListener('resize',onResize);page._cleanupPreview=function(){isRunning=!1;if(page._previewAnimId)cancelAnimationFrame(page._previewAnimId);window.removeEventListener('resize',onResize);if(renderer&&renderer.domElement&&renderer.domElement.parentNode){renderer.domElement.parentNode.removeChild(renderer.domElement)}}}
window.setupHomeCharacterPreview=setupHomeCharacterPreview;function updateRemotePlayerAnimation(p,dt){if(!p||p.isDead)return;const prevX=p.pos.x;const prevZ=p.pos.z;p.pos.lerp(p.targetPos,0.35);p.rotationY=THREE.MathUtils.lerp(p.rotationY,p.targetRotY,0.3);p.mesh.rotation.y=p.rotationY;const vx=(p.pos.x-prevX)/Math.max(0.001,dt);const vz=(p.pos.z-prevZ)/Math.max(0.001,dt);const speed=Math.hypot(vx,vz);const prevVx=p.vel.x;const prevVz=p.vel.z;p.vel.x=vx;p.vel.z=vz;const ax=(vx-prevVx)/Math.max(0.001,dt);const az=(vz-prevVz)/Math.max(0.001,dt);const kSquash=120.0;const dSquash=10.0;const fSquash=-kSquash*(p.squashScale.y-1.0)-dSquash*p.squashScale.vy;p.squashScale.vy+=fSquash*dt;p.squashScale.y+=p.squashScale.vy*dt;p.squashScale.xz=1.0/Math.sqrt(Math.max(0.2,p.squashScale.y));if(p.squashRoot){p.squashRoot.scale.set(p.squashScale.xz,p.squashScale.y,p.squashScale.xz)}
if(p.isDiving){p.mesh.rotation.x=THREE.MathUtils.lerp(p.mesh.rotation.x,Math.PI/2.2,0.25);p.mesh.rotation.z=THREE.MathUtils.lerp(p.mesh.rotation.z,0,0.25);if(p.leftArm){p.leftArm.rotation.x=-Math.PI*0.8;p.leftArm.rotation.z=0.2}
if(p.rightArm){p.rightArm.rotation.x=-Math.PI*0.8;p.rightArm.rotation.z=-0.2}
return}
if(p.isDown){p.mesh.rotation.z=THREE.MathUtils.lerp(p.mesh.rotation.z,Math.PI/2.1,0.2);p.mesh.rotation.x=THREE.MathUtils.lerp(p.mesh.rotation.x,Math.PI/6,0.15);if(p.headPivot){p.headPivot.rotation.x=Math.sin(Date.now()*0.008)*0.4+0.3;p.headPivot.rotation.z=Math.cos(Date.now()*0.006)*0.5}
if(p.pomPivot){p.pomPivot.position.x=Math.sin(Date.now()*0.015)*0.6;p.pomPivot.position.z=Math.cos(Date.now()*0.015)*0.6}
if(p.leftArm)p.leftArm.rotation.z=Math.PI*0.5+Math.sin(Date.now()*0.01)*0.3;if(p.leftElbow)p.leftElbow.rotation.z=Math.PI*0.4+Math.cos(Date.now()*0.012)*0.4;if(p.rightArm)p.rightArm.rotation.z=-Math.PI*0.5-Math.sin(Date.now()*0.01)*0.3;if(p.rightElbow)p.rightElbow.rotation.z=-Math.PI*0.4-Math.cos(Date.now()*0.012)*0.4;if(p.leftLeg)p.leftLeg.rotation.x=Math.PI*0.35+Math.sin(Date.now()*0.008)*0.2;if(p.leftKnee)p.leftKnee.rotation.x=-Math.PI*0.5;if(p.rightLeg)p.rightLeg.rotation.x=-Math.PI*0.2;if(p.rightKnee)p.rightKnee.rotation.x=-Math.PI*0.3;return}
p.mesh.rotation.x=THREE.MathUtils.lerp(p.mesh.rotation.x,0,0.2);p.mesh.rotation.z=THREE.MathUtils.lerp(p.mesh.rotation.z,0,0.2);const cosR=Math.cos(p.rotationY);const sinR=Math.sin(p.rotationY);const localAx=ax*cosR-az*sinR;const localAz=ax*sinR+az*cosR;const kSpine=80.0;const dSpine=8.5;const targetSpinePitch=-localAz*0.025+(speed>0.5?0.18:0);const targetSpineRoll=localAx*0.03;const fSpinePitch=-kSpine*(p.spineSpring.pitch-targetSpinePitch)-dSpine*p.spineSpring.vPitch;const fSpineRoll=-kSpine*(p.spineSpring.roll-targetSpineRoll)-dSpine*p.spineSpring.vRoll;p.spineSpring.vPitch+=fSpinePitch*dt;p.spineSpring.vRoll+=fSpineRoll*dt;p.spineSpring.pitch+=p.spineSpring.vPitch*dt;p.spineSpring.roll+=p.spineSpring.vRoll*dt;const kHead=65.0;const dHead=7.0;const targetHeadPitch=p.spineSpring.pitch*1.3-localAz*0.015;const targetHeadRoll=-p.spineSpring.roll*1.2;const fHeadPitch=-kHead*(p.headSpring.pitch-targetHeadPitch)-dHead*p.headSpring.vPitch;const fHeadRoll=-kHead*(p.headSpring.roll-targetHeadRoll)-dHead*p.headSpring.vRoll;p.headSpring.vPitch+=fHeadPitch*dt;p.headSpring.vRoll+=fHeadRoll*dt;p.headSpring.pitch+=p.headSpring.vPitch*dt;p.headSpring.roll+=p.headSpring.vRoll*dt;const kPom=140.0;const dPom=6.0;p.pomSpring.vx+=(-kPom*p.pomSpring.x-dPom*p.pomSpring.vx-localAx*0.1)*dt;p.pomSpring.vz+=(-kPom*p.pomSpring.z-dPom*p.pomSpring.vz-localAz*0.1)*dt;p.pomSpring.x+=p.pomSpring.vx*dt;p.pomSpring.z+=p.pomSpring.vz*dt;if(p.pomPivot){p.pomPivot.position.x=THREE.MathUtils.clamp(p.pomSpring.x,-0.3,0.3);p.pomPivot.position.z=THREE.MathUtils.clamp(p.pomSpring.z,-0.3,0.3);p.pomPivot.rotation.z=-p.pomSpring.x*2.0;p.pomPivot.rotation.x=p.pomSpring.z*2.0}
if(p.scarfTailPivot){p.scarfTailPivot.rotation.x=THREE.MathUtils.lerp(p.scarfTailPivot.rotation.x,Math.PI*0.15+speed*0.08+Math.sin(p.wobblePhase*2)*0.2,0.2);p.scarfTailPivot.rotation.z=Math.cos(p.wobblePhase)*0.2}
if(p.bellyMesh){const bellyWobble=Math.sin(p.wobblePhase*1.5)*0.12;p.bellyMesh.scale.set(0.9+bellyWobble,0.95-bellyWobble,0.6+bellyWobble*1.2)}
if(speed>0.4){p.wobblePhase+=dt*(12.0+speed*0.6);const legS=Math.sin(p.wobblePhase)*0.85;const armS=Math.cos(p.wobblePhase)*0.95;const bounceY=Math.abs(Math.sin(p.wobblePhase))*0.12;if(p.pelvisGroup){p.pelvisGroup.position.y=0.5+bounceY;p.pelvisGroup.rotation.z=Math.sin(p.wobblePhase)*0.12;p.pelvisGroup.rotation.y=Math.cos(p.wobblePhase)*0.15}
if(p.spineGroup){p.spineGroup.rotation.x=p.spineSpring.pitch;p.spineGroup.rotation.z=p.spineSpring.roll+Math.sin(p.wobblePhase)*0.08}
if(p.headPivot){p.headPivot.rotation.x=p.headSpring.pitch;p.headPivot.rotation.z=p.headSpring.roll+Math.cos(p.wobblePhase)*0.1}
if(p.leftArm){p.leftArm.rotation.x=-armS;p.leftArm.rotation.z=Math.PI*0.12+Math.sin(p.wobblePhase*0.5)*0.25}
if(p.leftElbow){p.leftElbow.rotation.x=Math.max(0,-armS*0.6);p.leftElbow.rotation.z=Math.sin(p.wobblePhase)*0.2}
if(p.rightArm){p.rightArm.rotation.x=armS;p.rightArm.rotation.z=-Math.PI*0.12-Math.sin(p.wobblePhase*0.5)*0.25}
if(p.rightElbow){p.rightElbow.rotation.x=Math.max(0,armS*0.6);p.rightElbow.rotation.z=-Math.sin(p.wobblePhase)*0.2}
if(p.leftLeg)p.leftLeg.rotation.x=legS;if(p.leftKnee)p.leftKnee.rotation.x=(legS<0)?-legS*0.9:0.1;if(p.rightLeg)p.rightLeg.rotation.x=-legS;if(p.rightKnee)p.rightKnee.rotation.x=(-legS<0)?legS*0.9:0.1}else{p.wobblePhase+=dt*3.5;const idleS=Math.sin(p.wobblePhase);const idleC=Math.cos(p.wobblePhase*0.8);if(p.pelvisGroup){p.pelvisGroup.position.y=0.5+idleS*0.04;p.pelvisGroup.rotation.z=idleC*0.06}
if(p.spineGroup){p.spineGroup.rotation.x=idleS*0.08;p.spineGroup.rotation.z=-idleC*0.06}
if(p.headPivot){p.headPivot.rotation.x=-idleS*0.1;p.headPivot.rotation.z=idleC*0.09}
if(p.leftArm){p.leftArm.rotation.x=THREE.MathUtils.lerp(p.leftArm.rotation.x,idleS*0.15,0.2);p.leftArm.rotation.z=THREE.MathUtils.lerp(p.leftArm.rotation.z,0.2+idleC*0.1,0.2)}
if(p.leftElbow)p.leftElbow.rotation.x=0.15+idleS*0.1;if(p.rightArm){p.rightArm.rotation.x=THREE.MathUtils.lerp(p.rightArm.rotation.x,-idleS*0.15,0.2);p.rightArm.rotation.z=THREE.MathUtils.lerp(p.rightArm.rotation.z,-0.2-idleC*0.1,0.2)}
if(p.rightElbow)p.rightElbow.rotation.x=0.15-idleS*0.1;if(p.leftLeg)p.leftLeg.rotation.x=THREE.MathUtils.lerp(p.leftLeg.rotation.x,0,0.2);if(p.leftKnee)p.leftKnee.rotation.x=THREE.MathUtils.lerp(p.leftKnee.rotation.x,0.05,0.2);if(p.rightLeg)p.rightLeg.rotation.x=THREE.MathUtils.lerp(p.rightLeg.rotation.x,0,0.2);if(p.rightKnee)p.rightKnee.rotation.x=THREE.MathUtils.lerp(p.rightKnee.rotation.x,0.05,0.2);}}
window.updateRemotePlayerAnimation=updateRemotePlayerAnimation;document.addEventListener('init',(event)=>{let page=event.target;if(page.id==='home-page'){initHomePage(event);page.addEventListener('show',scanPublicSlots);page.addEventListener('destroy',function(event){page.removeEventListener('show',scanPublicSlots)},{'once':!0});let quickmatchbtn_element=document.querySelector('#quick-match-btn');if(quickmatchbtn_element){quickmatchbtn_element.addEventListener('click',handleQuickMatch);page.addEventListener('destroy',function(event){quickmatchbtn_element.removeEventListener('click',handleQuickMatch)},{'once':!0})}
let createroomopenbtn_element=document.querySelector('#create-room-open-btn');if(createroomopenbtn_element){createroomopenbtn_element.addEventListener('click',handleOpenCreateModal);page.addEventListener('destroy',function(event){createroomopenbtn_element.removeEventListener('click',handleOpenCreateModal)},{'once':!0})}
let closecreatemodalbtn_element=document.querySelector('#close-create-modal-btn');if(closecreatemodalbtn_element){closecreatemodalbtn_element.addEventListener('click',handleCloseCreateModal);page.addEventListener('destroy',function(event){closecreatemodalbtn_element.removeEventListener('click',handleCloseCreateModal)},{'once':!0})}
let submitcreateroombtn_element=document.querySelector('#submit-create-room-btn');if(submitcreateroombtn_element){submitcreateroombtn_element.addEventListener('click',handleCreateRoomSubmit);page.addEventListener('destroy',function(event){submitcreateroombtn_element.removeEventListener('click',handleCreateRoomSubmit)},{'once':!0})}
let joinpasscodeopenbtn_element=document.querySelector('#join-passcode-open-btn');if(joinpasscodeopenbtn_element){joinpasscodeopenbtn_element.addEventListener('click',handleOpenPasscodeModal);page.addEventListener('destroy',function(event){joinpasscodeopenbtn_element.removeEventListener('click',handleOpenPasscodeModal)},{'once':!0})}
let closepasscodemodalbtn_element=document.querySelector('#close-passcode-modal-btn');if(closepasscodemodalbtn_element){closepasscodemodalbtn_element.addEventListener('click',handleClosePasscodeModal);page.addEventListener('destroy',function(event){closepasscodemodalbtn_element.removeEventListener('click',handleClosePasscodeModal)},{'once':!0})}
let submitjoinpasscodebtn_element=document.querySelector('#submit-join-passcode-btn');if(submitjoinpasscodebtn_element){submitjoinpasscodebtn_element.addEventListener('click',handleJoinWithPasscode);page.addEventListener('destroy',function(event){submitjoinpasscodebtn_element.removeEventListener('click',handleJoinWithPasscode)},{'once':!0})}
let solobotbtn_element=document.querySelector('#solo-bot-btn');if(solobotbtn_element){solobotbtn_element.addEventListener('click',handleStartBotGame);page.addEventListener('destroy',function(event){solobotbtn_element.removeEventListener('click',handleStartBotGame)},{'once':!0})}
let refreshroomsbtn_element=document.querySelector('#refresh-rooms-btn');if(refreshroomsbtn_element){refreshroomsbtn_element.addEventListener('click',scanPublicSlots);page.addEventListener('destroy',function(event){refreshroomsbtn_element.removeEventListener('click',scanPublicSlots)},{'once':!0})}
let openhelpbtn_element=document.querySelector('#open-help-btn');if(openhelpbtn_element){openhelpbtn_element.addEventListener('click',handleOpenHelpModal);page.addEventListener('destroy',function(event){openhelpbtn_element.removeEventListener('click',handleOpenHelpModal)},{'once':!0})}
let closehelpmodalbtn_element=document.querySelector('#close-help-modal-btn');if(closehelpmodalbtn_element){closehelpmodalbtn_element.addEventListener('click',handleCloseHelpModal);page.addEventListener('destroy',function(event){closehelpmodalbtn_element.removeEventListener('click',handleCloseHelpModal)},{'once':!0})}
let soundtogglebtn_element=document.querySelector('#sound-toggle-btn');if(soundtogglebtn_element){soundtogglebtn_element.addEventListener('click',handleSoundToggle);page.addEventListener('destroy',function(event){soundtogglebtn_element.removeEventListener('click',handleSoundToggle)},{'once':!0})}}});document.addEventListener('init',(event)=>{let page=event.target;if(page.id==='room-page'){initRoomPage(event);let addbotbtn_element=document.querySelector('#add-bot-btn');if(addbotbtn_element){addbotbtn_element.addEventListener('click',handleRoomAddBot);page.addEventListener('destroy',function(event){addbotbtn_element.removeEventListener('click',handleRoomAddBot)},{'once':!0})}
let roomreadytogglebtn_element=document.querySelector('#room-ready-toggle-btn');if(roomreadytogglebtn_element){roomreadytogglebtn_element.addEventListener('click',handleRoomToggleReady);page.addEventListener('destroy',function(event){roomreadytogglebtn_element.removeEventListener('click',handleRoomToggleReady)},{'once':!0})}
let roomstartbtn_element=document.querySelector('#room-start-btn');if(roomstartbtn_element){roomstartbtn_element.addEventListener('click',handleRoomStartGame);page.addEventListener('destroy',function(event){roomstartbtn_element.removeEventListener('click',handleRoomStartGame)},{'once':!0})}
let roomleavebtn_element=document.querySelector('#room-leave-btn');if(roomleavebtn_element){roomleavebtn_element.addEventListener('click',handleRoomLeave);page.addEventListener('destroy',function(event){roomleavebtn_element.removeEventListener('click',handleRoomLeave)},{'once':!0})}
let stickerbtn0_element=document.querySelector('#sticker-btn-0');if(stickerbtn0_element){stickerbtn0_element.addEventListener('click',handleStickerClick);page.addEventListener('destroy',function(event){stickerbtn0_element.removeEventListener('click',handleStickerClick)},{'once':!0})}
let stickerbtn1_element=document.querySelector('#sticker-btn-1');if(stickerbtn1_element){stickerbtn1_element.addEventListener('click',handleStickerClick);page.addEventListener('destroy',function(event){stickerbtn1_element.removeEventListener('click',handleStickerClick)},{'once':!0})}
let stickerbtn2_element=document.querySelector('#sticker-btn-2');if(stickerbtn2_element){stickerbtn2_element.addEventListener('click',handleStickerClick);page.addEventListener('destroy',function(event){stickerbtn2_element.removeEventListener('click',handleStickerClick)},{'once':!0})}
let stickerbtn3_element=document.querySelector('#sticker-btn-3');if(stickerbtn3_element){stickerbtn3_element.addEventListener('click',handleStickerClick);page.addEventListener('destroy',function(event){stickerbtn3_element.removeEventListener('click',handleStickerClick)},{'once':!0})}}});document.addEventListener('init',(event)=>{let page=event.target;if(page.id==='game-page'){initGamePage(event);let gamequitbtn_element=document.querySelector('#game-quit-btn');if(gamequitbtn_element){gamequitbtn_element.addEventListener('click',handleGameLeave);page.addEventListener('destroy',function(event){gamequitbtn_element.removeEventListener('click',handleGameLeave)},{'once':!0})}
let resultlobbybtn_element=document.querySelector('#result-lobby-btn');if(resultlobbybtn_element){resultlobbybtn_element.addEventListener('click',handleGameLeave);page.addEventListener('destroy',function(event){resultlobbybtn_element.removeEventListener('click',handleGameLeave)},{'once':!0})}
let resulttag0_element=document.querySelector('#result-tag-0');if(resulttag0_element){resulttag0_element.addEventListener('click',handleResultQuickTagClick);page.addEventListener('destroy',function(event){resulttag0_element.removeEventListener('click',handleResultQuickTagClick)},{'once':!0})}
let resulttag1_element=document.querySelector('#result-tag-1');if(resulttag1_element){resulttag1_element.addEventListener('click',handleResultQuickTagClick);page.addEventListener('destroy',function(event){resulttag1_element.removeEventListener('click',handleResultQuickTagClick)},{'once':!0})}
let resulttag2_element=document.querySelector('#result-tag-2');if(resulttag2_element){resulttag2_element.addEventListener('click',handleResultQuickTagClick);page.addEventListener('destroy',function(event){resulttag2_element.removeEventListener('click',handleResultQuickTagClick)},{'once':!0})}
let resulttag3_element=document.querySelector('#result-tag-3');if(resulttag3_element){resulttag3_element.addEventListener('click',handleResultQuickTagClick);page.addEventListener('destroy',function(event){resulttag3_element.removeEventListener('click',handleResultQuickTagClick)},{'once':!0})}
let resulttag4_element=document.querySelector('#result-tag-4');if(resulttag4_element){resulttag4_element.addEventListener('click',handleResultQuickTagClick);page.addEventListener('destroy',function(event){resulttag4_element.removeEventListener('click',handleResultQuickTagClick)},{'once':!0})}}})